# App/Agents — C6.4, the Agents panel

Owned by **C6.4** of the C6 composite spec,
`docs/doperpowers/specs/2026-09-07-c6-conversation-surface.md` (leaf table, "Four leaves, not
three"): the Agents tab of §8.8 — the run tree, the per-run transcript, node actions, chip
navigation. `AgentNavigation.swift` is the Y4 seam the orchestrator landed with the skeleton;
C6.4 replaces its default. No other leaf edits anything in this directory.
