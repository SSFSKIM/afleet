# App/Composer — C6.2, the composer

Owned by **C6.2** of the C6 composite spec,
`docs/doperpowers/specs/2026-09-07-c6-conversation-surface.md` (leaf table, "Four leaves, not
three"): the composer field, the router UI over X10, `@` and `!`, paste and drop, the queue chip,
edit via rewind, ghost text, the pickers and the bypass gate. No other leaf edits anything in this
directory.
