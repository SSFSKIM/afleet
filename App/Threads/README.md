# App/Threads — C6.3, the Thread tab

Owned by **C6.3** of the C6 composite spec,
`docs/doperpowers/specs/2026-09-07-c6-conversation-surface.md` (leaf table, "Four leaves, not
three"): the Thread tab and §7.5's five thread kinds, taken from C5's placeholder by contract Y3's
`unregister(.thread)` handover. No other leaf edits anything in this directory.
