Authored `gh --json` documents this leaf's GitHub tab decodes.

Real field names, invented values, never a recorded account (§11). The same rule and the same
shape as `SourceControlCoreTests/Samples`.
