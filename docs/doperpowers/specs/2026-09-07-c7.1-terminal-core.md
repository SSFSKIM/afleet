# C7.1: Terminal core — `TerminalSurface`, the PTY layer, the GhosttyKit adapter, S1 (2026-09-07)

> **Parent:** `docs/doperpowers/specs/2026-09-05-c7-workbench-panels.md`, leaf **C7.1**.
> **Parent-pin:** that path at commit `b775842` ("Spec: the human's two decisions at the C5
> merge; C7 cut approved, com.afleet.app is the identifier"), and the root spec
> `docs/doperpowers/specs/2026-09-03-afleet-workspace-design.md` at the same commit.
> **Level name:** leaf of the C7 composite, wave 2 (the package cores). **Track:**
> controlled — this document, then `docs/doperpowers/plans/2026-09-07-c7.1-terminal-core.md`,
> then execution. **Branch:** `child/c7-terminal-core`, worktree
> `../afleet-c7/terminal-core`; merges to `main` when G1, G2 and G3 pass. The architect
> merges; this child never pushes.
>
> This document treats the composite's C7.1 section, contracts W1, W2 and W8, the root
> spec's §9.3, §9.5, §9.6, X1 and X11, and the 2026-09-07 Revision Notes as **landed**, and
> records only the residue they leave. It also reads the C5 child spec
> `2026-09-06-c5-app-shell.md` §7 (the pane seam the exit report has to fit), C5's three
> Revision Notes on the composite, `Workbench/Package.swift` at the pin and
> `docs/tech-debt-tracker.md`. Engine facts are re-anchored to
> `~/claude-code-bundle/2.1.263/cli.pretty.js` with line numbers.

## Purpose

Everything a terminal pane needs below the panel, and nothing a panel would need above it:

1. **`TerminalSurface`** (contract W2) — the renderer seam. One protocol, so that which
   terminal emulator afleet renders with is one file's decision and not a commitment.
2. **The PTY layer** — an actor that opens a pty, spawns a process in a given directory
   with a given environment and nothing else, carries keystrokes in and bytes out, resizes
   the tty, and reports the child's end **once**, distinguishing an exit from a signal from
   a stop. The hatch's re-adoption (root §7.4, §7.2 rule 5) keys on that report, so it is
   the load-bearing deliverable: C4 already ships `PaneExit` and waits for it.
3. **The GhosttyKit adapter** over `InMemoryTerminalSession`, which is what makes the first
   two real on screen.
4. **S1** — the spike that settles whether GhosttyKit renders a login shell and the CLI's
   own attach client well enough to be the renderer, with SwiftTerm behind the same
   `TerminalSurface` as the named fallback.

After C7.1, C7.4 can build panes without knowing what a pty is, and the composite's items
15, 17 and 23 have their floor.

## Acceptance

The composite's three gates, restated as things a test or a person can observe. Four rules
bind all of them and are repeated in the plan's Global Constraints.

**Discriminating tests (root §17.7, binding).** Every test written to prove a fix is
demonstrated failing against the pre-fix code before the fix is accepted. Where the break
cannot be executed, the substitute is stated as such in the test's own comment. A test that
compares two things also asserts that it found them.

**Nothing is written under a config home (X9).** Not by this child's code, not by its tests.
The only writer under `/tmp/afleet-fixtures/config-home` is the `claude` this child spawns
in G2. Test trees live under `FileManager.default.temporaryDirectory`.

**Reports emit counts, and environments are asserted by name (root §6.3, §11).** No test,
failure message or report prints a path, an environment dump, a session id or a slug. An
environment assertion names the variables it expects and the variables it forbids, and reads
the child's answer as `present`/`absent` tokens the child prints — never as a dump.

**Zero model turns.** G2 attaches to a **promptless** `claude --bg --resume <id>`, which the
CLI documents as "continues that session in the background under the same ID"
(`claude --help`, 2.1.263). No prompt is ever sent. `AFLEET_LIVE_CLI_TURNS` is never read.

### G1 — the PTY layer, under `swift test --package-path Workbench` (required)

Every item is a headless XCTest in `TerminalCoreTests` with a real child process:

- **a.** `/bin/sh -c` in a temporary directory reports that directory back as its `pwd`, and
  reports a crafted variable's value; both read from the child's own stdout through the pty.
- **b.** **Nothing is inherited.** The test process sets a sentinel variable of its own
  before spawning; the child, given an environment that does not contain it, prints `absent`
  for the sentinel and `present` for exactly the names the request carried. The pre-fix
  demonstration is a spawn that passes `environ` through: it prints `present` and the test
  fails. Names only, both directions, never a dump.
- **c.** The child's initial window size is the one requested (`stty size`), and a resize
  after spawn is seen by the child as a new `TIOCGWINSZ` **and** as a `SIGWINCH` it can trap.
- **d.** The child is the session leader of its own session with the pty slave as its
  controlling terminal — proven from inside the child (`ps -o tpgid`/`tty` is not needed: an
  open of `/dev/tty` from the child succeeds and a `stty` read works, both of which fail
  without a controlling terminal), and by the layer's own `tcgetpgrp` on the master.
- **e.** **Exit, signal, stop, each observed and each reported once.** `exit 7` reports
  `.exited(7)`; `kill -TERM $$` reports `.signalled(15)`; `kill -STOP $$` reports a *stop*,
  which is not a termination, and under the detaching policy is followed by `SIGCONT` then
  `SIGHUP` and one terminal report of `.signalled(1)`. A counter proves each report fires
  exactly once even though the master's EOF and `waitpid` both fire.
- **f.** A spawn failure (an executable that does not exist, a cwd that does not exist) is a
  typed error at the call, never a report of an exit that never happened.

### G2 — S1, in the harness, against the installed CLI (required)

Run by hand from `swift run S1Harness` with `CLAUDE_CONFIG_DIR=/tmp/afleet-fixtures/config-home`
and `AFLEET_LIVE_CLI=1`, with the findings recorded in this document's Revision Notes:

- **a.** A login shell renders through the adapter: prompt, colour, `vim`-class full-screen
  redraw, live resize by dragging the window, and a CJK composition committed through IME.
- **b.** `claude attach <short>` against a promptless `claude --bg --resume <id>` job started
  under the scratch home renders the job's screen.
- **c.** Ctrl+Z returns to the pane, and `claude agents --json` still lists the job.
  **Whether the attach client exits or stops is recorded** — the bundle says it stops (below,
  *Engine facts*), and the live run either confirms that or overturns it.
- **d.** **Machine-checked where it can be** (added 2026-09-07 after the rendering probe
  below): the harness carries a self-test leg that feeds known bytes through the real PTY
  layer and asserts the rendered grid through `readViewportText()`, so "it rendered" is an
  assertion rather than a recollection. What genuinely needs a human stays with the human:
  the IME composition, the live drag, and whether it *looks* right.
- **Promote-or-fallback.** Three for three promotes GhosttyKit. Any failure adopts
  SwiftTerm's `TerminalView` behind `TerminalSurface` **with the PTY layer unchanged**,
  recorded as a Revision Note here, and G2 re-runs against it.

### G3 — a flooding child cannot freeze the host (required)

**The discriminator moved, 2026-09-08.** G3a was specified as a main-actor latency bound
under a `yes` flood. Measurement showed that bound cannot fail: synchronous per-read delivery
— the defect it was written against — still returns 3.878 ms median and 4.902 ms maximum
across 112 MB in 1,024-byte reads. The gate therefore rests on the two structural properties
that *do* fail under mutation — coalesced delivery, and the bounded buffer under a consumer
that stops reading — with the latency bound and the byte floor retained as labelled
regression guards rather than as evidence. The finding is the useful part: on this hardware
the renderer, not the hop count, is what a flood can threaten, which is why G2's harness leg
still matters and why the byte-per-hop cap is expressed in bytes rather than in hops.

- **a.** Headless, in `TerminalCoreTests`: with a `yes` child flooding for ten seconds
  through the real PTY layer into a stand-in surface, a main-actor heartbeat scheduled every
  50 ms is serviced with a median latency under 50 ms and a maximum under 500 ms, and the
  count of heartbeats delivered is within 10% of the count scheduled. The pre-fix
  demonstration is a synchronous main-thread hop per read, which misses the bound.
- **b.** In the harness, the same `yes` child for ten seconds leaves the window draggable and
  the pane responsive to a keystroke; the observation is recorded as a Revision Note, since
  no test can witness it.

## Grounding baseline

Measured 2026-09-07 in this worktree at the pin, on the toolchain the composite's baseline
names (macOS 26.5.2, Xcode 26.6, Swift 6.3.3).

- **The package builds and libghostty-spm resolves.** `swift build --package-path Workbench`
  succeeds in 21.9 s from the pin's `Package.resolved`, 257 build units, including
  `GhosttyKit`, `GhosttyTerminal` and the `libghostty` binary xcframework
  (`upstream.c4e16970a803`). The pin is `exact: "1.5.20260903"` and **is not bumped by this
  child** (human gate, 2026-09-07).
- **The manifest** carries C5's `PanelHostAPITests` line inside the `PanelHostAPI` fence
  (composite Revision Note 2026-09-06). C7.1's region holds `TerminalCore` and
  `TerminalCoreTests` with one placeholder source each.
- **`TerminalCore` cannot see `PaneRequest`.** Its declared dependencies are `AfleetCore`,
  `GhosttyTerminal` and `GhosttyKit` — not FleetKit, where `PaneRequest` and `PaneExit` live
  (`FleetKit/Sources/FleetSessions/Types/Panes.swift`). The PTY layer therefore takes its own
  value type and C7.4 maps `PaneRequest` onto it; see *The pane seam* below.
- **`PaneExit` carries one `Int32 code` and no signal field**, and C4 accepts an exit only
  when `exit.request.id` matches the one it waits on (`Panes.swift:37-46`, C5 spec §7).
- **libghostty-spm's in-memory backend, read in the resolved checkout.**
  `InMemoryTerminalSession` is a `@unchecked Sendable` final class constructed with `write:`,
  `resize:` and `suppressesPixelOnlyResizes:`; it exposes `receive(_:)` (Data and String),
  `sendInput(_:)`, `finish(exitCode: UInt32, runtimeMilliseconds: UInt64)`,
  `readViewportText() -> String?` and `waitForPendingOutput()`. `setSurface`/`clearSurface`
  are internal, so **the session is inert until a view attaches one**: `readViewportText()`
  returns `nil` with no surface, which is what makes an adapter unit test possible headlessly
  and what makes rendering G2's job rather than G1's.
- **The AppKit view.** `TerminalView` is a typealias for `AppTerminalView`, an `open class
  AppTerminalView: NSView` with `configuration: TerminalSurfaceOptions`, `controller:
  TerminalController?`, `NSTextInputClient` conformance (IME), `fitToSize()`, and public
  `sendText`/`sendKey`/`paste`. `TerminalSurfaceOptions.backend` defaults to `.exec`; the
  adapter sets `.inMemory(session)`. It satisfies W2's `var view: NSView` directly.
- **The name `TerminalSurface` is already taken by the dependency.** `GhosttyTerminal`
  exports `public final class TerminalSurface`
  (`Sources/GhosttyTerminal/Surface/TerminalSurface.swift:17`), together with
  `TerminalSurfaceView`, `TerminalSurfaceOptions` and eighteen
  `TerminalSurface*Delegate` protocols. W2 names our protocol `TerminalSurface` in a module
  that imports that one. Residue Q1.
- **The terminfo entry Ghostty needs is not part of macOS.** `xterm-ghostty` resolves on this
  machine only from a per-user database in `$HOME/.terminfo`, installed by a Ghostty
  application this project does not require; `/usr/share/terminfo` has no such entry. The
  package ships its own copy at
  `Sources/GhosttyTerminal/Resources/terminfo/{78/xterm-ghostty, 67/ghostty}`, delivered as a
  SwiftPM resource bundle. Residue Q4, and a `[parent-impact]` below.
- **The pty mechanism is measured, not assumed (2026-09-07, macOS 26.5.2).** A C spike
  outside the repository established three things the Design rested on. (a) `posix_spawn`
  with `POSIX_SPAWN_SETSID`, the slave path opened as descriptor 0 in the child and dup'd to
  1 and 2, **does** give the child a controlling terminal: the child's `tty` names the pty and
  `exec 3</dev/tty` succeeds, and `tcgetpgrp(master)` returns the child's group. The
  composite's `forkpty` fallback is therefore not needed and the risk it hedged is closed.
  (b) `ioctl(master, TIOCSWINSZ)` reaches the child as a new size **and** as `SIGWINCH`.
  (c) **The trap:** if the parent closes its slave descriptor before the child reopens the
  slave by name, macOS resets the pty's window size to `0x0`, and a `TIOCSWINSZ` issued on
  the master in that window is discarded — the child then reads `0 0` from `stty size`
  forever. The parent must hold its slave descriptor open across the `posix_spawn` and close
  it afterwards, while the child still opens the slave *by name* (a dup'd descriptor never
  becomes a controlling terminal). Both halves are load-bearing. This also makes the
  composite's suggested `stty size` check dangerous if written loosely: G1c asserts exact
  numbers, because `0 0` is what a wrong implementation reports and "some size" would pass.
- **The terminal-describing overlay works, measured end to end (2026-09-07).** The package
  exposes a supported accessor — `GhosttyRuntimeResources.terminfoDirectoryURL` — and it
  resolves in an unbundled `swift run` binary, from a sibling `GhosttyKit_GhosttyTerminal.bundle`
  with the database in a flat `terminfo/` holding `67/ghostty` and `78/xterm-ghostty`. The
  decisive check is the one a pane's child actually performs: a process given
  `TERM=xterm-ghostty` and `TERMINFO_DIRS` pointing at that directory, and **nothing else**,
  read the entry successfully (`infocmp` exit 0, 3,073 bytes). So the `[parent-impact]`'s
  remedy is effective and not merely plausible: a pane on a machine with no Ghostty
  installation gets a working terminal description. Tasks 4 and 5 use that accessor — no
  bundle scanning, no hard-coded path — and the degradation to `xterm-256color` applies only
  if it ever returns `nil`.
- **G2's zero-cost job is verified end to end (2026-09-07).** In the scratch config home,
  with an explicitly built environment (`env -i` plus `HOME`, `PATH`, `TERM` and
  `CLAUDE_CONFIG_DIR` only — no `CLAUDE_CODE_*` marker from this harness), a promptless
  `claude --bg --resume <id>` in that session's own project directory answered
  `backgrounded · <short> (idle — send a prompt to start)`. It stays resident and spends
  nothing, exactly as the live-cost statement claims. `claude agents --json` then listed one
  entry, `state: "blocked"`, with the right `cwd`; the entry's field names are `cwd`, `id`,
  `kind`, `name`, `pid`, `sessionId`, `startedAt`, `state`, `status` — note there is **no**
  `short` field, so a reader wanting the short id takes it from `id`. The job was stopped
  afterwards with `claude stop` and the roster returned to zero entries. Nothing in this
  child's own process wrote under the config home; the spawned engine did, which is what X9
  permits.
- **GhosttyKit renders in an unbundled `swift run` executable, and the host can read the
  grid back (2026-09-07, isolated scratch package).** A probe outside the repository built an
  `executableTarget` against the pinned `libghostty-spm`, set
  `NSApplication.setActivationPolicy(.regular)`, put an `AppTerminalView` in a programmatic
  `NSWindow`, attached an `InMemoryTerminalSession`, fed it two lines and read them back:
  `readViewportText()` returned exactly `hello from the probe` and `second line`. The
  `resize` closure fired twice with real grid dimensions (16x46, then 25x93 once the window
  laid out), which is the callback that drives `onResize` and therefore `TIOCSWINSZ`. Three
  consequences. (a) The S1 harness needs no application bundle, as the composite's decision
  assumed — now measured rather than assumed. (b) **A trap that would have cost Task 5 a
  day:** the view creates no surface at all unless `controller` is set —
  `TerminalSurfaceCoordinator.rebuildIfReady()` returns early on a `nil` controller — so the
  adapter must set one, and the failure mode is a fully wired pane that renders nothing with
  no error anywhere. (c) `readViewportText()` makes most of S1 **machine-checkable** rather
  than eyeball-only; see the Acceptance note on G2.
- **Darwin grants the controlling terminal at `setsid`, not at the slave's `open` — so W2's
  stated mechanism is wrong on macOS, and G1d cannot discriminate the way the plan expected
  (2026-09-07).** Two further C variants of the same reference settle it. (a) Opening the
  slave with `O_NOCTTY` **still** leaves the child with a working `/dev/tty`, a correct
  `tcgetpgrp`, and the right size — POSIX says `O_NOCTTY` prevents acquisition; Darwin does
  not honour it here. (b) Dropping the open-by-name entirely and `dup2`-ing the *inherited*
  slave descriptor onto 0, 1 and 2 also yields a controlling terminal, which POSIX would not
  grant either, since a `dup` is not an `open`. Both were run after the Task 2 worker
  reported (a) from Swift, and both confirm it independently. Consequences: **only
  `POSIX_SPAWN_SETSID` is load-bearing for the controlling terminal on this platform**, so
  G1d rests on that mutation plus an honestly-labelled trace assertion rather than on two
  behavioural mutations as the plan asked; and W2's advisory sentence that the slave "opened
  after `setsid`, without `O_NOCTTY` ... makes the slave the session leader's controlling
  terminal **at that open**" describes POSIX rather than macOS. The implementation keeps the
  open-by-name shape anyway — it is the portable one, it is what W2 recommends, and it is
  already built and tested; the `dup2` shape is recorded only so a later reader knows the
  simplification exists and why it was not taken.
- **Stop, signal and the detach sequence, measured in the same spike.** Through the pty
  above: `exit 7` reports `EXITED code=7`; `kill -TERM $$` reports `SIGNALLED signal=15`;
  `kill -STOP $$` reports `STOPPED` under `waitpid(WUNTRACED)` with the child left alive; and
  `SIGCONT` followed by `SIGHUP` to the child's process group then reports `SIGNALLED
  signal=1` — the detaching policy's exact sequence, confirmed before a line of it was
  written. **A numbering trap came with it:** macOS is BSD-numbered, so `SIGSTOP` is 17 and
  `SIGTSTP` is 18, while the signal table embedded in the engine bundle (`:397658`) is
  Linux-numbered (19 and 20). Tests and code use the `SIGSTOP`/`SIGTSTP`/`SIGHUP` constants
  and never a numeric literal; a test that hard-coded 19 would pass on the wrong platform and
  fail here for a reason that has nothing to do with the pane.
- **The scratch config home** at `/tmp/afleet-fixtures/config-home` holds transcripts for
  many recorded sessions under `projects/-private-tmp-afleet-fixtures-*`, two job records
  under `jobs/`, and a completed onboarding — everything G2's promptless resume needs. This
  child reads it and never writes to it.

### Engine facts, re-anchored to 2.1.263

The composite's attach facts were read from 2.1.258; the root spec's 2026-09-07 note asks
for a re-anchor before C7. Re-read in `~/claude-code-bundle/2.1.263/cli.pretty.js`:

- **`claude attach <id>`** is still "Open the background session in this terminal. ←
  returns to agent view, Ctrl+Z drops back to your shell. The session keeps running either
  way." (`:822906`). The attach screens still say "Ctrl+Z to detach" (`:362478`, `:363177`,
  `:536126`) and a finished job still prints "— done · Ctrl+Z to return —" (`:363268`).
- **SUPERSEDED 2026-09-08 by G2's live run — the paragraph below is what the bundle text
  supported, and the running client does not behave that way.** Ctrl+Z on `claude attach` in a
  pane pty produced `.ended(.exited(code: 0))` in four consecutive runs, with a no-Ctrl+Z
  control proving the byte causal; no `.stopped` ever arrived. The error was mine and it was an
  over-generalisation: `handleSuspend` is the **generic** renderer's key handler, and I read it
  as the attach client's without evidence that the attach path routes there. The product text
  quoted just above — "Ctrl+Z drops back to your shell. The session keeps running either way" —
  reads at least as naturally as the attach process *exiting* while the daemon keeps the job,
  which is what was observed. Kept in place, marked, because it is why `.detach` and
  `WUNTRACED` exist and both remain correct for any other child that does stop.
- **Ctrl+Z is a suspend, not an exit — from the bundle, ahead of S1.** The renderer's input
  pump routes `ctrl+z` to `handleSuspend()` (`:137944`), which drops raw mode, emits
  `suspend`, installs a `SIGCONT` handler and calls `process.kill(0, "SIGTSTP")` — the whole
  **process group** (`:137890`). The gate on that path (`:137689`) disables it only when
  `CLAUDE_CODE_SESSION_KIND === "bg"`, i.e. inside a background *host*, never in the
  foreground attach *client*. **Consequence, and it is a design input rather than a
  curiosity:** in a pane there is no shell to run `fg`, so a detaching attach client leaves a
  *stopped* child on the pty. `waitpid` without `WUNTRACED` would report nothing at all and
  the pane would hang forever with a live process and no event. G1e and the detaching policy
  exist for this; G2 confirms it live.
- **The detach constants are message codes, not exit statuses.** `SUPERVISOR_DETACH_CODE`,
  `TRANSIENT_ATTACH_CODE`, `FATAL_ATTACH_CODE` and their neighbours are regular expressions
  and strings over `E[A-Z]+:` error names (`:189083`, exported at `:478587`). Nothing in the
  bundle promises a numeric process exit code for a detach, so the pane cannot pre-decide
  what a given exit status means and must key on the *event* (exit, signal, stop), which is
  what W2 already says and what C4's `PaneExit` already consumes.
- **`--bg` with `--resume`** is documented as "continues that session in the background under
  the same ID, or starts a copy and says so when the session is already running"
  (`claude --help`, 2.1.263) — the promptless, zero-turn job G2 attaches to.

## Design

W2 is the design and is carried whole. This section records only what building it fixes.

### The PTY layer

`PTYProcess`, an actor in `TerminalCore`, owns one pty master and one child.

```
PTYSpawnRequest    executable: URL, arguments: [String], cwd: URL,
                   environment: [String: String]        // complete; X11's composition, not ours
                   size: TerminalSize
                   terminal: TerminalDescription        // TERM and an optional terminfo directory
                   stopPolicy: .report | .detach
PTYEvent           .output(Data) | .stopped(signal: Int32) | .ended(PTYTermination)
PTYTermination     .exited(code: Int32) | .signalled(signal: Int32)
                   var paneExitCode: Int32              // code, or 128 + signal (shell convention)
```

The recommended mechanism of W2 is adopted unchanged: `openpty(3)`, then `posix_spawn` with
`POSIX_SPAWN_SETSID` and file actions that open the slave path as descriptors 0, 1 and 2 in
the child so the slave becomes the new session's controlling terminal at that open;
`ioctl(TIOCSWINSZ)` on resize; a `DispatchSource` read loop on the master on a private queue;
`waitpid` with `WUNTRACED`. `forkpty(3)` stays the named fallback and G1d is the test that
would send us there.

**Three things this fixes that W2 left open.**

1. **A stop is not an end.** `.stopped` is reported as its own event and the child stays
   alive. Under `stopPolicy: .detach` — which C7.4 selects for an attach pane — the layer
   answers a stop with `SIGCONT` then `SIGHUP` to the child's process group and the child's
   real termination follows as the single `.ended`. Under `.report` — the default, and what a
   shell pane uses — the pane decides. The policy lives here rather than in the panel because
   the signalling is process-group work the panel has no handle for, and it is a *parameter*
   rather than a rule because SIGHUP-ing a plain shell pane the user suspended would be a
   defect, not a detach.
2. **Exactly one `.ended`.** The master's EOF and `waitpid`'s answer race on every exit.
   **As shipped the actor requires *both* before it emits** — not "emits the first and swallows
   the second", which is what this paragraph said until the whole-branch review checked it
   against the code. The stricter rule is the better one and it is what W2 is amended from, so
   the sentence is corrected here rather than the code. The reviewer hunted for a case where the
   two diverge — a `sleep &` grandchild, redirected descriptors, and a helper that `setsid()`s
   and re-opens the slave by name while the direct child exits — and Darwin's session-leader tty
   revoke delivered EOF within about 10 ms in all three, so no reachable case waits on the
   difference. G1e's counter is the test.
3. **Terminal-describing variables are overlaid on the request's environment.** See the
   `[parent-impact]` below. The set is exactly `TERM`, and `TERMINFO_DIRS` when the surface
   supplies its own database; a test pins the set by name so an addition is deliberate.

### `TerminalSurface` and the adapter

The protocol is W2's, with two additions recorded as *Parent revisions* below: it is
`@MainActor` (it vends an `NSView`, which is main-actor-isolated in Swift 6, so the
annotation states what the compiler already requires), and it gains a read-only
`terminalDescription: TerminalDescription` so the PTY layer can be told what the renderer
*is* without the panel knowing either.

`GhosttyTerminalSurface` implements it over `AppTerminalView` with
`TerminalSurfaceOptions(backend: .inMemory(session))`. `feed` calls `receive`; the session's
`write` closure drives `onInput`; its `resize` closure drives `onResize`;
`processDidExit(code:)` calls `finish(exitCode:runtimeMilliseconds:)` with the elapsed time
the adapter measured. The two closures are invoked by the session off the main actor, so the
handler pair lives in one lock-guarded box owned by the adapter — the single-owner-box escape
the root spec's concurrency conventions permit, documented at the site.

`suppressesPixelOnlyResizes` is left **off** (the package's default): W2's `TerminalSize`
carries pixel dimensions, so a host that stopped seeing sub-cell changes would report stale
pixel metrics to `TIOCSWINSZ` forever. `resizeThrottleMilliseconds` is left at 0 for the same
reason, and revisited only if G2 shows a live drag outrunning a full-screen TUI.

### Backpressure: what G3 actually requires

The read loop never touches the main actor per read. Bytes are drained on the private queue
into a bounded buffer and delivered to the surface in coalesced chunks, one main-actor hop
per delivery, with a cap on bytes per hop. Nothing is dropped: a producer faster than the
renderer is bounded by the pty's own flow control, which is the correct back-pressure and the
one a terminal is designed around. G3a measures the property (main-actor responsiveness)
rather than a proxy for it (a hop count, which a slow renderer would also satisfy).

### The pane seam (W8, X7) — what C7.4 inherits

`TerminalCore` does not import FleetKit and therefore never sees a `PaneRequest`. C7.4 maps
`PaneRequest {id, executable, arguments, cwd, environment, purpose}` onto `PTYSpawnRequest`,
choosing `stopPolicy: .detach` for `.attach` and `.report` otherwise, and maps the single
`.ended` event onto `PaneExit(request:code:observedAt:)` using `PTYTermination.paneExitCode`.
That mapping is one function and it is C7.4's; `paneExitCode` is provided here so C7.4 does
not invent the convention. `PaneExit` cannot distinguish `.signalled(7)` from `.exited(135)`;
that is acceptable and is recorded as an observation for the architect rather than a change,
because X5's re-adoption keys on the *event*, not on the number.

### The S1 harness

`Workbench/Spikes/S1Harness`, an `executableTarget` in C7.1's manifest region, not in the
`Workbench` product. It sets `NSApplication.setActivationPolicy(.regular)` and activates —
an unbundled `swift run` executable is otherwise a background application and never receives
key events or an input method, which would make G2a's IME check untestable for the wrong
reason. It opens one `NSWindow` programmatically, hosts one adapter and one `PTYProcess`, and
takes its command from arguments so the same binary runs the login shell, the `yes` flood and
`claude attach` legs. It prints counts and durations, never paths.

## Residue, and how it is answered

Every question the inherited material leaves, with the answer this child takes. Q1–Q5 are
decided here; Q6 is the one the architect may want to see before execution.

**Q1. `TerminalSurface` collides with a public class of the dependency.**
*Answer (decided).* Keep W2's name — it is binding, C7.4 consumes it, and the collision is
containable. `TerminalCore` declares the protocol; within that module the local declaration
wins unqualified lookup, and the adapter file spells the dependency's type
`GhosttyTerminal.TerminalSurface` wherever it means that one. `TerminalCore` does **not**
`@_exported import GhosttyTerminal`, so no consumer ever sees both. Task 4 carries a test
that names both types in one file, so a future toolchain that stops permitting the shadowing
fails a test rather than a merge. If it ever does fail, the answer is more qualification, not
a rename.

**Q2. What does the PTY layer report for a signal, given `PaneExit.code: Int32`?**
*Answer (decided).* A typed `PTYTermination` here, plus `paneExitCode` (code, or 128 + signal)
so the lossy projection has exactly one implementation and it is tested. The composite's "the
code or signal" is honoured in full inside `TerminalCore`, and the loss happens at C4's
boundary, once, visibly.

**Q3. Is "a stopped attach child is detached" the PTY layer's rule or the pane's?**
*Answer (decided).* The mechanism is the layer's, the choice is the request's:
`stopPolicy: .report | .detach`, default `.report`. Reasons in the Design above. The
composite's sentence is satisfied — a stopped attach child *is* treated as detached — without
imposing it on a shell pane.

**Q4. `TERM` and terminfo.** The request's environment is the user's, composed by C4; the
pty is ours. A Ghostty-rendered pane handed the user's `TERM=xterm-256color` renders through
a description of a different terminal, and one handed `TERM=xterm-ghostty` with no database
breaks every ncurses program on a machine with no Ghostty installed — which is the ordinary
machine, since the entry is not part of macOS.
*Answer (decided, and filed as a `[parent-impact]`).* The layer overlays exactly the
terminal-describing variables the surface declares — `TERM`, and `TERMINFO_DIRS` prepended
with the renderer's own database directory when it has one — onto the request's environment,
and a test pins that set by name. This is not inheritance from afleet's environment, which is
what X11 forbids; it is the description of a device the layer just created.

**Q5. Can the adapter be tested without a window?**
*Answer (decided).* Yes, for the half we own: a real `InMemoryTerminalSession` with no
surface attached exercises `write` → `onInput`, `resize` → `onResize`,
`processDidExit` → `finish`, and proves `feed` with no surface neither crashes nor blocks.
`readViewportText()` returns `nil` there, so *rendering* is asserted only in G2. The plan
states which claims the headless tests can and cannot make, so nobody later mistakes green
tests for a rendered terminal — the lesson C4 and C5 both paid for.

**Q6. How much of `AppTerminalView` does the pane need, and does a second surface conform?**
*Answer (recommended, and the one place the architect may prefer otherwise.* W2 says the
protocol is "kept to what a pane needs and nothing the renderer happens to offer", and this
child keeps it there: six members plus `terminalDescription`. Selection, scrollback search,
hyperlink hover, bell, progress and pwd reporting are all available on `AppTerminalView` and
are all **left out**, so that the SwiftTerm fallback is a day's work rather than a rewrite,
and so that C7.4 cannot quietly become Ghostty-specific. The cost is that a later pane
feature is a W2 amendment. Recommendation: accept the cost.

## What C7.4 inherits from the adapter (recorded 2026-09-08, from Task 5)

Two facts the panel leaf would otherwise have to rediscover, both found by building it:

- **Each surface owns its own `TerminalController`; they must not share one.** The obvious
  reading — and the one this child's own rendering probe used — is
  `TerminalController.shared`. That is wrong for panes: the controller carries the
  instance-level appearance API, so a shared one makes a theme or font change on any pane
  alter **every** pane, and leaks that state into surfaces created afterwards. The adapter
  therefore installs a per-instance controller, and a test pins it: the shared-controller
  mutation reports `untouched-surface-font=changed` and `new-surface-theme=inherited`.
  Remember that a nil controller creates no surface at all, so "just don't set one" is not an
  option either.
- **Clipboard policy is set by the adapter, and any confirmation *UI* is C7.4's.** A bare
  `AppTerminalView` **denies protected user paste** when no confirmation delegate is
  installed, so a pane without one silently refuses Cmd-V. The adapter therefore mirrors the
  dependency's documented default: user-initiated paste is allowed, programmatic OSC 52
  clipboard reads and writes are denied. W2 deliberately exposes no confirmation-UI seam, so
  the adapter does not invent one — a panel that wants to prompt the user owns that, and the
  policy above is the floor it builds on. (Task 5's reviewer proposed adding the UI here; it
  was dismissed as a child-boundary crossing, and this paragraph is the hand-off instead.)

- **The ordered ingress is the seam C7.4 wires to — use it, not `Task { await write }`.**
  Ships on `PTYProcess` as:

  ```swift
  public nonisolated func sendInput(_ data: Data)
  public nonisolated func sendResize(to size: TerminalSize)
  ```

  `TerminalSurface.onInput` and `onResize` are **synchronous** callbacks the renderer invokes
  off the main actor, while `write` and `resize` are `async`. Wrapping each callback in its own
  unstructured `Task` — which is what the harness did until the whole-branch review — leaves N
  writes racing to enter the actor with nothing deciding the winner: typing `ab` quickly enough
  delivers `ba` (measured, 3 of 3 runs), and a stale grid can win the last `TIOCSWINSZ`
  (2 of 3, inherently probabilistic). The write gate cannot help, because its promise is about
  the order callers *entered the actor*, not the order the surface produced them; **and resize
  was never covered by that gate at all**, so input-versus-resize ordering was previously
  unspecified. The ingress is therefore **one queue for both**, which is what makes "the last
  grid the surface produced is the last one the child is told about" true. Both calls are
  fire-and-forget: a synchronous callback has nowhere to return an error, and a failing write
  means the pty is already closed. A caller that needs the error, or needs to know the bytes
  landed, still uses `write(_:)` / `resize(to:)`.
- **Knowing a theme name was wrong: `GhosttyTerminalSurface.themeResolution`**
  (`.systemAppearance` / `.named` / `.unknownName`). `TerminalAppearance.themeName == nil`
  means "follow the system", and an unknown name previously fell back to the same visual result,
  so a typo was indistinguishable from the default and nobody learned. The visual fallback is
  unchanged; only the caller can now tell them apart. **Deliberately on the concrete adapter,
  not on `TerminalSurface`** — W2 keeps the protocol to what a pane needs and nothing the
  renderer happens to offer, C7.4 holds the concrete type, and widening a binding protocol costs
  a parent revision. If C7.4 needs it through the protocol, that is a one-line W2 amendment then,
  on evidence, rather than speculation now.
- **`.ended` is delivered on the main actor, which C4 should know.** Every event, the terminal
  one included, is yielded from a main-actor hop. A consumer that is *not* main-actor-isolated
  still cannot observe `.ended` while the main actor is blocked — a test blocks it 250 ms on
  purpose and delivery stops for that long. This is deliberate (the surface is main-actor
  isolated and the pane renders there), but X5's re-adoption keys on that event, so a hatch
  handed back during a long main-actor stall sees the exit late rather than never. Recorded by
  the whole-branch review.

## `[parent-impact]` — the pty's own environment is not the request's environment alone

**What the parent says.** W2, marked `[binding — X11 and the lifecycle seam]`: the PTY layer
"spawns with the channel's cwd and `ResolvedEnvironment.variables` as the complete
environment (no inheritance from afleet's own)". X11 says the same of every spawn.

**Why it cannot be kept literally.** `TERM` in the resolved environment describes the
terminal emulator the *user's login shell* was captured under — Terminal.app, iTerm, or the
Claude Code session afleet was launched from. The pane's child is talking to a pty this layer
just opened, rendered by libghostty. Handing it the captured `TERM` misdescribes the device;
handing it `xterm-ghostty` without a database breaks every curses program, because that entry
is not shipped by macOS and resolves on this machine only from a per-user database installed
by a Ghostty application afleet does not require. Either way item 23's promise — that a pane
behaves as the user's terminal does — fails for a reason the user cannot see.

**What this child does.** The PTY layer overlays exactly the terminal-describing variables
the surface declares (`TERM`, and `TERMINFO_DIRS` with the renderer's bundled database
prepended when it has one) over the request's environment, pinned by name in a test. Nothing
else is added and nothing from afleet's own environment is inherited.

**The amendment recommended to the architect.** W2 and X11 gain the clause: *"…as the
complete environment, except for the terminal-describing variables the surface requires
(`TERM`, and `TERMINFO_DIRS` when the renderer supplies its own terminfo database), which
describe the pty the layer just created rather than any environment afleet inherited; the set
is pinned by a test."* Filed rather than edited: three leaves share the composite.

## Parent revisions (advisory; the architect applies them at merge)

1. **W2's protocol is `@MainActor`.** `var view: NSView` makes it so under Swift 6 whatever
   the snippet says; writing the annotation is a clarification, not a change.
2. **W2's protocol gains `var terminalDescription: TerminalDescription { get }`.** The PTY
   layer must be told what terminal it is driving (see the `[parent-impact]`), and the
   renderer is the only thing that knows. A separate refinement protocol was rejected: it
   would exist only to avoid editing text the architect edits at merge anyway, and C7.4
   consumes the source, not the snippet. The SwiftTerm fallback satisfies it
   (`xterm-256color`, no database of its own).
3. **W2's mechanism sentence is POSIX, not macOS.** Its recommendation that the slave's
   `open` after `setsid` is what makes the pty the controlling terminal does not describe
   this platform: Darwin grants it at `setsid`, and neither `O_NOCTTY` nor a `dup2`-only
   shape changes the outcome (measured; Grounding baseline). The recommendation still leads
   to correct code and the child keeps it, so this is a factual correction to the parenthetical
   rather than a change of design — but a later leaf debugging a pty on another platform
   should not read that sentence as a measured fact about this one.
4. **W1's target table gains `GhosttyTheme` for `TerminalCore`.** The row lists AfleetCore,
   `GhosttyTerminal` and `GhosttyKit`; the appearance mapping needs the named-theme product
   from the same pinned package. Added inside this leaf's own manifest region — an added
   product, not a new dependency edge, and no other leaf's region touched.
5. **W2 gains the stop policy.** `PTYSpawnRequest.stopPolicy` with the default `.report` and
   `.detach` for attach panes, per Q3.
6. **Observation, no change asked.** `PaneExit.code` cannot distinguish a signalled child
   from an exit with the same number. Recorded because a reader of C4's tests may otherwise
   assume it can.

## Delegated unknowns

- **S1 (G2).** Owned here, settled by the harness, promote-or-fallback per the composite, and
  recorded as a Revision Note either way. **Settled 2026-09-08, and the bundle's answer was
  wrong:** the live run observed a clean exit, not a stop. This is precisely what the gate was
  for — a prediction read off engine text, overturned by watching the engine — and it is the
  reason a bundle reading is evidence about the bundle, never about the running client.
- **Nothing else is delegated.** The controlling-terminal acquisition, which the composite
  lists as a risk, is settled inside G1d rather than deferred: if `posix_spawn` with
  `POSIX_SPAWN_SETSID` does not produce a controlling terminal on macOS 26, G1d fails and the
  named `forkpty` fallback is taken inside the same task, with a Revision Note.

## Live-cost statement

**Zero model turns, zero dollars.** G2 attaches to a promptless `claude --bg --resume <id>`
against a session the scratch home already has a transcript for; a promptless resume stays
resident and sends nothing to the model. No test and no harness leg sends a prompt.
`AFLEET_LIVE_CLI_TURNS` is never read. The child's environment for every spawn is built
explicitly, never inherited from the harness this child runs under, because
`CLAUDE_CODE_CHILD_SESSION` in that environment switches the engine's session registration
off (root §6.1, 2.1.263 `:96526`, `:193423`) and would make the job invisible to
`claude agents --json` for a reason that has nothing to do with the pane.

## Contracts

- **W1** — owner of `Workbench/Package.swift`. This child edits its own region and adds the
  `S1Harness` executable target there; C5's `PanelHostAPI` fence and every other leaf's
  region are left byte-for-byte alone.
- **W2** — owner. The protocol, the PTY layer's observable contract, the adapter.
- **X11** — bound, with the `[parent-impact]` above.
- **X1** — `TerminalCore` imports `AfleetCore` and the Ghostty products only; never
  `ClaudeWire`, never FleetKit. An import test proves it.
- **W8/X7** — consumed at one remove: the exit report must fit `PaneExit`, and it does.

## Deferred / out of scope

Panes, tabs, the hatch, `claude logs`, job attach as a *product* (all C7.4); split panes;
scrollback search, selection, hyperlink hover, bell and pwd reporting; a host-managed
byte-stream feed (the parent reserves it; `feed` already takes bytes); a `libghostty-vt`
Swift-renderer adapter; any bump of the libghostty-spm pin (human gate: a bump is a Revision
Note, never an implicit track).

## Decision Log

- Decision: keep W2's `TerminalSurface` name despite the dependency's class of the same name;
  contain it by never re-exporting `GhosttyTerminal` and qualifying at the two sites that
  need the other type.
  Rationale: the name is binding and C7.4 consumes it; a rename would ripple into the
  composite for a problem that a module boundary already solves. Rejected: renaming ours to
  `PaneSurface`; renaming by `typealias` (moves the ambiguity, keeps it).
  Date/Author: 2026-09-07 / C7.1.

- Decision: a stop is a first-class event, and answering it with SIGCONT+SIGHUP is a
  per-request policy rather than a rule of the layer.
  Rationale (as argued at the time; its premise about the attach client was later overturned
  by G2, and the decision still stands on the rest): a pane must observe stops or hang, because
  any child may stop itself — G1e tests exactly that — while SIGHUP-ing a shell pane the user
  suspended would destroy work. What changed is only that the attach client is no longer the
  motivating case: it exits cleanly.
  Rejected: always detaching on stop (the composite's literal reading); reporting stops and
  leaving all policy to the panel (the panel has no process-group handle).
  Date/Author: 2026-09-07 / C7.1.

- Decision: overlay `TERM` (and `TERMINFO_DIRS`) rather than pass the request's environment
  untouched.
  Rationale: in the `[parent-impact]` above. Rejected: shipping our own terminfo copy into a
  config-home-adjacent path (X9-adjacent, and unnecessary — the package already ships one);
  forcing `xterm-256color` always (throws away the renderer's real capabilities, and misreports
  the emulator to programs that ask).
  Date/Author: 2026-09-07 / C7.1.

- Decision: G3 is a headless test of main-actor responsiveness under a real flood, not only a
  harness observation.
  Rationale: the composite asks for the property; a harness-only check would be unrepeatable
  and would not run in `swift test`, which parent acceptance item 6 requires to cover the PTY
  layer. Rejected: counting main-thread hops (a proxy; the root spec's own warning about
  instruments that measure something correlated with the property).
  Date/Author: 2026-09-07 / C7.1.

- Decision: the S1 harness sets `NSApplication.setActivationPolicy(.regular)`.
  Rationale: an unbundled `swift run` executable is a background application; without this it
  never becomes key, never receives an input method, and G2a's IME leg would fail for a
  reason that says nothing about the renderer. Rejected: building an app bundle by hand
  (C5 owns packaging); deferring IME to C7.4.
  Date/Author: 2026-09-07 / C7.1.

## Tracking

Tech-debt entries filed by this child are numbered **82 through 96** under a `## From C7.1`
heading. Nothing above is renumbered. A number outside that range belongs to another leaf.

## Outcomes & Retrospective

Pending — written when the child closes, before the architect merges.

## Revision Notes

- 2026-09-08: **S1 ran. GhosttyKit is recommended for promotion, and the recommendation is
  not final until three legs a person has to witness are witnessed.** The harness is one
  process per leg — `.regular` activation policy, one programmatic `NSWindow`, one
  `GhosttyTerminalSurface`, one `PTYProcess` — with `selftest`, `flood`, `shell` and
  `attach <short>` chosen by argument, and it prints counts and durations only. It starts no
  engine: the promptless job was started by hand outside it, and its children's environments
  are composed by name (`HOME`, `PATH`, and `CLAUDE_CONFIG_DIR` for the attach leg) so no
  `CLAUDE_CODE_*` marker of the harness's own can reach one. Two traps the Grounding baseline
  named were both real and both already handled by the adapter: a nil `controller` creates no
  surface, and a per-surface controller is what keeps `setAppearance` from being process-wide.
  A third turned up in the harness itself: the surface's **first** grid report is the view's
  pre-layout default (16x46 here, the pair the rendering probe recorded), and a child spawned
  on it paints its first screen for a window that is not on the display, so the harness waits
  for the reports to stop before it spawns.

  **`selftest` — G2d, and it passes.** Two invented markers fed through the real PTY layer
  came back from `readViewportText()` in the rendered grid (2/2 markers, 48 bytes, one
  delivery, grid 33x112). A programmatic window resize then travelled the whole way and back:
  33x112 → 41x138 reported through `onResize` in 59.5 ms across three grid reports, and the
  child's own `stty size` on `SIGWINCH` rendered *the same pair* into the viewport. That last
  step is what makes this an assertion about the loop rather than about the callback: view
  layout, `onResize`, `TIOCSWINSZ`, the child's signal, its output, the grid.

  **`flood` — G3b, observed, with a finding that matters more than the pass.** Ten seconds of
  a `yes` child through the real PTY layer into the real renderer, four runs: main-run-loop
  heartbeat median 17.3–23.6 ms, maximum 117–262 ms, 2.4–9.4 MB delivered in 395–789
  deliveries; the window's frame still changed on command in every run; a real `NSEvent`
  keystroke sent through the window came back out of the session's write callback in 24.9,
  50.3, 253.9 and 587.1 ms. Time spent inside `feed` totalled 251–347 ms of the ten seconds
  (longest single call 35–72 ms), so **the host's delivery is not the cost — the renderer is.**
  Held against the same ten seconds headless in `TerminalCoreTests` (median 2.025 ms, maximum
  10.115 ms, 199/200 beats, 129,932,356 bytes) the renderer costs roughly an order of
  magnitude in run-loop latency and caps throughput about fifteen-fold. The 50 ms heartbeat
  loses 16–24% of its ticks under the flood, which G3a's headless ratio would call a failure
  and this leg does not: a tick displaced by the renderer's own draw pass is a frame budget,
  not a backpressure defect. What it does mean is concrete and should not be lost — a window
  dragged during a full-rate flood will hitch by up to a quarter of a second. The window is
  responsive, not smooth, and no test can tell those apart.

  **`attach` — G2b and G2c, and the bundle's prediction is overturned.** Against a promptless
  `claude --bg --resume` job started by hand under the scratch config home, the attach client
  rendered its screen into the pane: 14 non-blank rows of a 33x112 grid from 2.0–3.9 KB in
  2–4 deliveries. **Ctrl+Z, delivered as the 0x1A byte a tty carries for it, was followed by
  `.ended(.exited(code: 0))` — an exit, not a stop — 762–789 ms later, in four consecutive
  runs.** No `.stopped` event was ever reported, and the `.detach` policy therefore never
  fired; a run under `.detach` observed the same clean exit, with nothing for the policy to
  answer. The control run is what makes the byte causal rather than coincidental: the same leg
  with `--hold`, which sends no Ctrl+Z, left the client rendering and running for the full
  twenty seconds with no event at all. `claude agents --json` listed the job after every one
  of those runs, and the job was stopped afterwards with `claude stop`, leaving the roster at
  zero entries as it was found. The bundle's reading (2.1.263 `:137944`, `:137890`) said the
  client routes `ctrl+z` to a handler that sends `SIGTSTP` to its whole process group, and the
  spec carried that as the prediction S1 would confirm. It did not. **What is established is
  what the PTY layer observed — a clean exit — not the mechanism behind it**: the pane's child
  is a session leader in a process group that is orphaned with respect to the harness, and
  POSIX discards a `SIGTSTP` that would stop a process in an orphaned process group, so
  whether the client never sent the signal or sent one the kernel dropped and then exited on
  its own path is not settled by this run and was not investigated further. The consequence
  for the design is the good one either way, and it is smaller than the fallback the prediction
  justified: on this platform a detaching attach client leaves no stopped child on the pty, and
  the pane sees the ordinary single `.ended`. `.detach` and `WUNTRACED` stay — a stop is still
  reachable from any other child, `kill -STOP` included, and G1e tests it directly — but they
  are no longer load-bearing for *this* client.

  **Verdict: promote GhosttyKit — provisionally.** Everything machine-checkable in G2 holds:
  the renderer renders through the adapter over a real pty, the grid can be read back and
  asserted, a resize round-trips to the child, a background job's screen renders, and Ctrl+Z
  returns a clean event the pane can act on. Nothing failed, so the fallback is not triggered
  and SwiftTerm was not adopted. Three legs of G2a remain **unwitnessed and are not claimed**:
  a CJK composition committed through IME, a live resize by dragging the window, and whether
  the rendering looks right — colour, font, cursor, and a `vim`-class full-screen redraw. A
  screen capture was attempted to stand in for the last of these and was refused by the
  machine ("could not create image from display"), which is the correct outcome: this agent
  did not see the screen. What the harness could check about the shell leg without a person it
  did: a login shell's first paint filled 1 non-blank row of a 33x112 grid from 473 bytes,
  which is a prompt and nothing more. The verdict is final when a person has run
  `swift run --package-path Workbench S1Harness shell` and reported those three.

- 2026-09-08: **a correction to how this document's overlay claim was held.** The Grounding
  baseline's measurement stands as written — it was run with an explicit
  `env -i TERM=xterm-ghostty TERMINFO_DIRS=<dir> infocmp -1 xterm-ghostty`, naming the term
  directly — but the *test* written to hold that claim inside the package was vacuous, and a
  targeted review caught it. macOS `/bin/sh` is bash 3.2 in sh mode and sets `TERM=dumb` as a
  shell variable whenever `TERM` is absent from the environment; `dumb` resolves from
  `/usr/share/terminfo`, so deleting the `TERM` overlay entirely left the child's `infocmp`
  check passing. It proved that *some* terminal name resolved, never the one the surface
  handed it. The check now compares `TERM` against the declared name before resolving it, and
  the deletion mutation fails. Two related gaps closed with it: the exactness pin had only
  ever run against a non-Ghostty description, so a renderer-conditional addition
  (`COLORTERM` when the term is `xterm-ghostty`) survived every mutation, and
  `TERMINFO_DIRS`'s absence was pinned by arithmetic rather than by name — a mutation that
  swapped one for the other kept the count and passed. The review's verdict on the
  `[parent-impact]` itself was **yes**: the name-set pin is genuine two-directional,
  value-sensitive evidence, `TERMINFO_DIRS` composes rather than clobbers (the renderer's
  directory is prepended, the user's retained), and nothing from afleet's own environment is
  reachable on the spawn path.
- 2026-09-07: **the architect's rulings at the spec gate.** The `[parent-impact]` is
  **accepted**: the PTY layer overlays exactly `TERM` and `TERMINFO_DIRS` (the package's
  bundled database) by name and takes everything else from the resolved environment; W2 and
  X11 gain the clause at merge and the two-directional name-pinned test is the proof. All
  four *Parent revisions* are **accepted** as advisory overturns and land on the composite at
  merge. Keeping W2's `TerminalSurface` name, contained by never re-exporting
  `GhosttyTerminal` and qualifying at the two sites, is confirmed. One addition to this
  child's fence, since C7.1 owns the manifest: the package-wide X1 import test today walks
  only `Sources/PanelHostAPI` (C7.3's ledger observation), so C7.1 makes it walk every
  target's sources under `Workbench/Sources` as a denylist over the ClaudeWire modules —
  C7.2 adding `PanelHostAPI` to `LinkRouting`'s dependencies is not a violation.

- 2026-09-07: v1, written at the controlled-track spec step against the composite at
  `b775842`. Engine facts re-anchored from 2.1.258 to 2.1.263. One `[parent-impact]` (the
  terminal-describing environment overlay) and four *Parent revisions* filed; six residue
  questions answered, five of them decided locally.
