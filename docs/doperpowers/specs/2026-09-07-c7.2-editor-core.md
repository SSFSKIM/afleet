# C7.2: Editor core — the Monaco bundle, bridge and view, `LinkRouter`, S3 (2026-09-07)

> **Parent:** `docs/doperpowers/specs/2026-09-05-c7-workbench-panels.md`, leaf **C7.2**.
> **Parent-pin:** that path at commit `b775842` ("Spec: the human's two decisions at the C5
> merge; C7 cut approved, com.afleet.app is the identifier"), and the root spec
> `docs/doperpowers/specs/2026-09-03-afleet-workspace-design.md` at the same commit.
> **Level name:** leaf of composite child C7, wave 2 of the v1 roadmap. **Track:** controlled
> (child spec → plan → execute). **Branch:** `child/c7-editor-core`, worktree
> `../afleet-c7/editor-core`; merges to `main` when G1 through G3 pass, by the architect.
>
> This document treats the composite's C7.2 section and its inheritance as landed and records
> only the residue those sections leave to this leaf. It also reads the composite's Revision
> Notes of 2026-09-06 in full — the `PanelHostAPI` test target, the
> `(WorkspaceLink, LinkDestination)` handler signature, and `LinkRouterCapability`'s
> `unregister(tab:)` withdrawal with its `async` `PanelHost.unregister(_:)` — the root spec's
> §9.1, §9.6, §6.3, §11, contracts X1, X7 (as amended 2026-09-07 at C5's merge) and X11, the
> C5 child spec `2026-09-06-c5-app-shell.md` Design §7, the shipped
> `Workbench/Sources/PanelHostAPI` sources, `App/Panels/HostLinkRouter.swift`, and
> `docs/tech-debt-tracker.md` at the same commit.

## Purpose

Two things every later Workbench leaf stands on, and neither of them is a panel.

The first is an editor: VS Code's own Monaco, built once by a script into a committed bundle,
loaded by an `NSView` over a `WKWebView`, and spoken to over one small JSON vocabulary. C7.5
opens a file in it at a line and saves it; C7.7 renders a commit's diff in it. Neither of them
should have to know what a `WKURLSchemeHandler` is, and neither should discover at integration
that the bridge means something different from what the other assumed.

The second is a router: `LinkRouter`, the registry every panel and C6 register a
`WorkspaceLink` claim against. C5 shipped the app's `HostLinkRouter` before this leaf existed
and wrote into its own source that when this module lands the host **delegates** to it rather
than a second registry coming into being. That hand-over is this leaf's, not C7.5's.

After C7.2 the whole Workbench package still shows nothing on a screen. What it has is the two
seams the four panel leaves would otherwise each invent a corner of.

## Acceptance

The composite's three gates, restated with the evidence each one produces. Nothing here
weakens them; where a clause cannot be literally true, it is filed below rather than quietly
reinterpreted.

### G1 — the package tests (required)

`swift test --package-path Workbench` passes and covers:

1. **The bridge codec round trip for every message in W4.** Six host-to-editor messages
   (`open`, `setText`, `gotoLine`, `setTheme`, `showDiff`, `save`) and five editor-to-host
   messages (`ready`, `dirty`, `saveRequested`, `cursor`, `error`), each round-tripped, each
   also **decoded from a literal JSON string**, and one test asserting that every `type` string
   the Swift codec emits appears in the committed bootstrap JavaScript. The round trip alone
   cannot fail on the defect it exists to catch — Swift agrees with itself whatever the
   JavaScript does — so the literal strings and the bootstrap check are what make it
   discriminating.
2. **`LinkRouter` over every `WorkspaceLink` case** (`.file`, `.diff`, `.url`, `.commit`,
   `.pullRequest`, `.command`): the registered target is called with the link intact **and the
   destination it was opened from**, the fallback with nothing registered, the specificity rule,
   and the tie-break by canonical `PanelTabID` order tested separately from specificity, because
   a comparison written only against unequal specificities passes with the tie-break missing.
   Withdrawal by tab is tested here too: after `unregister(tab:)` the link falls through to the
   next target and the withdrawn one receives nothing further.
3. **A resource test**: the bundle is present in `Bundle.module`, its `VERSION` names the pinned
   Monaco version, the licence is beside it, and the entry points the view loads (the document,
   the bootstrap and the five workers) all exist.

Evidence: the test count before and after, and the failing-first run of each new test (§17.7).

### G2 — S3, the spike (required)

The `S3Harness` executable, run with `swift run --package-path Workbench S3Harness`, reports:

- **cold load under one second from `swift run`** — measured from process start to the editor's
  `ready`, and separately from navigation start to `ready`, gating on the first because it is
  what the composite names and the stricter of the two;
- **a 5 MB file opens warm with no visible jank** — instrumented as time from `setText` to the
  next render callback plus a `requestAnimationFrame` frame-time histogram (p50, p95, worst)
  over a scripted scroll. "Visible" is a human's word: the harness prints the numbers and leaves
  the window open, and the human witness is named as such in the closing report rather than
  claimed by a number;
- **a 2,000-line diff renders**;
- **the worker-loading finding**: whether Monaco's editor and language workers start under the
  custom scheme, from Blob URLs, or only under `loadFileURL(_:allowingReadAccessTo:)`.

The 5 MB file and the diff pair are generated by the harness at run time from a deterministic
synthetic generator. Nothing is committed and no byte of either comes from an engine transcript
(§11).

Promote when the three measurements hold. Otherwise the composite's fallback in its stated
order, with the measurements recorded either way, as a Revision Note here.

### G3 — the bundle is committed, clean, and builds offline (required)

`git ls-files` shows the bundle, its `VERSION` and its licence, the build script and its
lockfile, and nothing else generated: no `node_modules`, no bun cache. A **fresh `git clone` of
this branch into a temporary directory** builds and tests with the network denied.

The offline claim is bounded, and the bound is filed as a `[parent-impact]` below rather than
smuggled into the wording: SwiftPM must still resolve `libghostty-spm`, which it does from the
machine-level repository cache. The proof is therefore "no network beyond a warm SwiftPM
dependency cache", run under `sandbox-exec` with `network*` denied — verified 2026-09-07 that
this does deny it (a `curl` under that profile fails DNS, exit 6). A machine that has never
fetched `libghostty-spm` still needs that one fetch, and no arrangement of this leaf changes
that.

## Grounding

Measured in this worktree on 2026-09-07 unless stated.

- **Baseline.** `swift test --package-path Workbench` passes with 8 tests, all of them C5's
  `PanelHostAPITests`. `make check-x7` is clean (27 requirements across 6 protocols, 3
  documents) and `make check-app-wiring` reports 18 allowlisted and 0 unwired.
  `Workbench/Package.resolved` is committed. `bun` 1.3.14 is installed.
- **Monaco 0.56.0 is the latest release** and is MIT; the npm package unpacks to 97,911,464
  bytes in 1,909 files, which is the composite's figure confirmed from the registry rather than
  recopied.
- **Monaco 0.56.0 has been restructured since the layout the composite's Grounding Baseline
  cites.** `monaco-editor`'s `exports` map is `"./*.js": "./esm/vs/*.js"`, so the ESM entry
  points are spelled `monaco-editor/editor/editor.main.js` and not `monaco-editor/esm/vs/…`;
  `esm/vs/editor/editor.all.js` no longer exists; and `editor.main.js` **already is** the set
  W4 asks for — it imports all ~85 basic-language `register.js` shims (each of which loads its
  grammar through a lazy `import()`), re-exports the `css`, `html`, `json` and `typescript`
  language features, and pulls every editor and diff-editor contribution. It also re-exports a
  new `external/monaco-lsp-client` as `lsp`. The older
  `basic-languages/monaco.contribution.js` and `language/*/monaco.contribution.js` paths still
  resolve as shims.
- **The bundle's measured size** (`bun build` of `editor.main.js` plus the five worker entries,
  `--splitting --format=esm --minify --target=browser`): **13 MB across 107 files**, of which
  `ts.worker.js` is 6.47 MB, and **2.91 MB compressed**, which is what the git objects cost.
  Next largest are two shared editor chunks at 2.25 MB and 1.13 MB, `css.worker.js` at 0.71 MB
  and `html.worker.js` at 0.39 MB; 103 of the 107 files are JavaScript, three are CSS and one
  is the codicon TrueType font.
- **SwiftPM's repository cache** (`~/.swiftpm/cache/repositories`) already holds
  `libghostty-spm`, and `sandbox-exec -p '(version 1)(allow default)(deny network*)'` denies
  network to a child process without touching machine state or needing `sudo`.
- **The shipped X7 seam** is `Workbench/Sources/PanelHostAPI/LinkRouterCapability.swift`:
  `LinkDestination` (`currentPanel`, `newWindow`), `LinkTarget` (`tab: PanelTabID`,
  `specificity: Int`, `handles: @Sendable (WorkspaceLink) -> Bool`,
  `open: @MainActor @Sendable (WorkspaceLink, LinkDestination) async -> Void`), and
  `LinkRouterCapability` with `register`, `unregister(tab:)` and `open(_:from:)`, all `async`.
- **C5's `HostLinkRouter`** is `@MainActor final class`, holds `private var targets:
  [LinkTarget]`, resolves highest specificity with ties broken by `PanelTabID`'s canonical
  order, pops the target's tab out before delivering when the destination is `.newWindow`, and
  falls back to `NSWorkspace.shared.open` for `.url` and to a diagnostic naming the kind for the
  other five. Its `openExternally` and `diagnostic` seams have no test injecting them today;
  its `targetCount` is read only by `AppTests/PanelHostTests.swift`.
- **Bundle engine facts.** This leaf needs none: nothing in it speaks the protocol, spawns a
  process, or reads a transcript. No line of `~/claude-code-bundle/2.1.263/cli.pretty.js` is
  cited because none applies, and inventing a citation would be worse than saying so.

## Design inheritance

- §9.6's `WorkspaceLink`, `DiffRef` and `DiffRef.Base`: **binding** (parent). Consumed, never
  redeclared.
- W4's bundle contents, the scheme-handler-and-`WKScriptMessageHandler` shape and the message
  vocabulary "and no other": **binding** (composite). The scheme name, the worker-loading route
  and the theme mapping are advisory within it.
- W5's registry-not-a-switch, the `(link, destination)` handler, the fallback and the
  specificity rule: **binding** (composite), as sharpened by X7's 2026-09-06 and 2026-09-07
  amendments.
- W1's manifest fences: **binding**. This leaf edits `Workbench/Package.swift` only between
  `// MARK: - C7.2` and `// MARK: - end of C7.2`. One row of W1's dependency table is
  contradicted by a later amendment on the same document; that is filed below, not edited here.
- X1's package edges, X7's protocol as C5 shipped it, §11's committed-build-output and
  publication rules, §6.3's assertion discipline: **binding** (parent).
- §9.1's bundling and bridge beyond what W4 fixes, and the Web Inspector rule: advisory.

## Design

Everything the composite and X7 already fix is carried whole. This section records only the
residue, each item with the reasoning that settles it.

### 1. What "tree-shaken" resolves to in 0.56.0

One document entry and five worker entries, built by `Tools/build-monaco.sh` with `bun build`:

```
src/editor.js       -> monaco-editor/editor/editor.main.js, plus this leaf's bootstrap
src/editor.worker.js, ts.worker.js, json.worker.js, css.worker.js, html.worker.js
```

`editor.main.js` is W4's list exactly, as the Grounding records, so the tree-shaking is a
consequence of the entry points rather than a hand-curated import list that would drift at
every Monaco bump. **Code splitting stays on**: the ~85 language grammars are behind lazy
`import()`s, and splitting is what keeps them out of the cold-load path. It also means the
bundle is a directory of 107 files rather than a handful, which is the shape the resource test
and the scheme handler are written for.

`monaco-lsp-client` arrives as part of `editor.main.js` and is left in place; excluding it would
mean curating the entry by hand for a saving the measurement says is not there.

### 2. The bundle, its stamp and its licence

`Workbench/Sources/EditorCore/Resources/monaco/`, declared in the manifest as
`resources: [.copy("Resources/monaco")]`. **`.copy`, not `.process`**: a web bundle's directory
layout *is* its URL space, and `.process` reserves the right to rewrite it. Read at run time
through `Bundle.module`.

Beside it: `VERSION`, naming the Monaco version, the bun version and the build date, which the
resource test compares against a constant in Swift; and Monaco's MIT licence text, copied from
the package. `Tools/monaco/package.json` and `bun.lock` are committed so the build is
reproducible; `node_modules` never is.

**The size question the human gate reserved is answered before the gate, by measurement**: 13 MB
across 107 files, 2.91 MB compressed. That is under the 20 MB threshold at which this leaf was
told to stop and report, so the bundle is committed and the leaf proceeds. If a later Monaco
bump pushes it over, the lever is `ts.worker.js`: dropping the TypeScript language service while
keeping `json`, `css` and `html` removes about half the bundle, and that is a W4 change, not a
local one.

### 3. `LinkRouter` — where it lives, what it owns, and what it refuses to own

`LinkRouter` is an **`actor`** in `LinkRouting`. The 2026-09-06 Revision Note kept
`unregister(tab:)` `async` expressly so this could be an actor rather than main-actor-isolated
state, and left the choice here; this leaf takes it. Serialised execution is also what makes the
withdrawal-before-registration ordering that note is about structural rather than argued.

It owns registration, withdrawal by tab, resolution and the fallback:

- **Resolution.** Highest `specificity` wins; ties break by `PanelTabID`'s canonical order, so
  the choice is total and independent of registration order. This is the rule `HostLinkRouter`
  already ships — moved, not re-decided.
- **Withdrawal.** `unregister(tab:)` drops every target that tab registered.
- **Fallback.** `.url` with nothing registered goes to an external opener; the other five cases
  emit a diagnostic **naming the kind and never the link**, because a path, a commit hash or a
  PR title in a log is exactly what §11 forbids. Both are `@Sendable` closures given at `init`
  with production defaults (`NSWorkspace.shared.open`, an `OSLog` line), so the fallback is
  testable without opening a browser and without the app, which is what W5's "pure and tested
  without the app" asks for.

It does **not** own pop-out. `.newWindow` has to pop the target's tab out before the target
delivers, and that needs the host. A stored host hook on an actor the host owns is a retain
cycle, so the hook is **per call**:

```swift
public func open(_ link: WorkspaceLink, from destination: LinkDestination,
                 prepare: (@MainActor @Sendable (LinkTarget, LinkDestination) async -> Void)? = nil) async
```

`prepare` runs after the target is resolved and before it is delivered to, which is the ordering
the `.newWindow` rule is about. With no `prepare` the router is the pure registry W5 describes
and its own tests use it that way.

### 4. The host delegates; a second registry never exists

`HostLinkRouter` keeps its `LinkRouterCapability` conformance, its weak host reference and its
pop-out logic, and delegates registration, withdrawal, resolution and the fallback to one
`LinkRouter`. Its `private var targets` is deleted — that array is the second registry X7's
amendment forbids.

Two consequences inside this leaf's fence, both in `App/Panels/HostLinkRouter.swift` and its
tests:

- `targetCount` becomes `async`, because the count now lives on an actor. Its only caller is
  `AppTests/PanelHostTests.swift`.
- The `openExternally` and `diagnostic` seams move to `LinkRouter.init`, so `HostLinkRouter`
  gains `init(externalOpener:diagnostic:)` with production defaults and `PanelHostModel`
  continues to construct it with none.

The single-registry claim is structural — it rests on a property being deleted — and no test can
witness the absence of a field. That is stated rather than papered over with a test that would
pass either way. What the tests do witness is behavioural parity: C5's link tests, including the
withdrawal-and-specificity one, pass unchanged against the delegated implementation, plus a new
app-side test that a `.url` nobody claimed reaches the injected external opener through the
delegated router.

### 5. The bridge codec, and the test that can actually fail

Flat tagged JSON — `{"type": "open", "path": …, "language": …, "text": …, "line": …}` — because
the other side of the wire is hand-written JavaScript that a Swift-shaped nested encoding would
make needlessly awkward to read. Two enums with custom `Codable`:

```
EditorCommand  (host -> editor)   open, setText, gotoLine, setTheme, showDiff, save
EditorEvent    (editor -> host)   ready, dirty, saveRequested, cursor, error
```

Exactly W4's vocabulary "and no other". Host-to-editor goes out through `evaluateJavaScript`;
editor-to-host arrives on a `WKScriptMessageHandler` named `afleet`.

The round-trip test W4 asks for is necessary and not sufficient: Swift encoding and decoding
its own enum agrees with itself no matter what the JavaScript believes. So each message is also
decoded from a **literal JSON string** written the way the bootstrap writes it, and one test
asserts every `type` string in the codec appears in the committed bootstrap source. That last
test is the one that fails when the two sides drift, which is the only failure this codec has.

### 6. `MonacoEditorView`

An `NSView` owning a `WKWebView`. The bundle is served through a `WKURLSchemeHandler` on the
`afleet-editor` scheme (advisory in W4; kept). The view is not exercised by `swift test` — a web
view in a package test buys a slow test that proves the framework works — and is exercised by
the S3 harness instead, which is what the spike is for.

`isInspectable` is `true` under `#if DEBUG`; in Release it is a settable property the app sets
from the Developer setting, so `EditorCore` reads no setting of its own. `setTheme {name}` passes
a Monaco built-in through (`vs`, `vs-dark`, `hc-black`, `hc-light`) and the default follows
`NSApp.effectiveAppearance`; any UI over it is C7.5's.

### 7. S3 measures three load paths, not two

The composite names two: the document and the workers. Code splitting adds a third —
**dynamic-import chunks**, which is how ~85 language grammars arrive — and a chunk fetch under
`afleet-editor:` is a load the scheme handler must service just as much as the document is. A
spike that measured only the document and the workers could promote a bundle whose Swift syntax
highlighting silently never loads.

The order of routes stays the composite's, with chunks measured at each step:

1. scheme for document, chunks and workers;
2. Blob-URL workers built by the bootstrap, document and chunks still on the scheme;
3. `loadFileURL(_:allowingReadAccessTo:)` for everything.

A fourth resort exists and is named so it is never reached silently: `MonacoEnvironment.getWorker`
returning nothing, which runs the language services on the main thread and leaves syntax
highlighting unaffected, because highlighting is not a worker's job. If S3 ever reaches it, it is
a Revision Note here and a degraded promotion, not a pass.

### 8. The S3 harness

`executableTarget(name: "S3Harness", dependencies: ["EditorCore"], path: "Spikes/S3Harness")`,
inside the C7.2 fence, in no product and not in the `Workbench` umbrella. It opens an `NSWindow`
from `swift run` with no app bundle, per the composite's decision. Its fixtures — the 5 MB file
and the 2,000-line diff pair — are generated at run time and never committed.

## `[parent-impact]` — W1's dependency table and X7's amendment contradict each other

W1's table, which is inside a `[binding]` block, gives `LinkRouting` a dependency on `AfleetCore`
alone. The composite's own later Revision Notes (2026-09-06) put the registry's whole vocabulary
in `PanelHostAPI`: `LinkTarget`, its `(WorkspaceLink, LinkDestination)` handler, and withdrawal
keyed by `PanelTabID`. A registry over those types cannot be built without importing that target.

The table row predates the seam. It was written when `LinkRouter` was imagined as a switch over
`WorkspaceLink` alone; X7 then made the target a `PanelTabID`-tagged, destination-carrying value,
and nothing went back to the table.

**What this leaf does:** adds `PanelHostAPI` to `LinkRouting`'s dependencies inside the C7.2
fence. The edge is acyclic — `PanelHostAPI` depends on `AfleetCore` and `FleetKit` only — and it
changes nothing for the panel leaves, which already depend on both.

**Rejected:** re-declaring `LinkTarget` and a tab identifier inside `LinkRouting` (two
`LinkTarget` types is precisely the duplication X7's amendment forbids); making `LinkRouter`
generic over a `Tab` parameter that would only ever be given `PanelTabID`, to preserve the
wording of a table row rather than its intent.

**For the architect at merge:** amend W1's table row for `LinkRouting` to read `AfleetCore,
PanelHostAPI`.

## `[parent-impact]` — "with no network" cannot be literally true of a clean checkout

C7.2's G3 says a clean checkout "builds and tests with the network disabled", and the
composite's Parent-Level Acceptance item 6 says the package tests pass "from a clean checkout
with no network". Neither can be literally true: `Workbench` depends on `libghostty-spm` by URL,
and a checkout on a machine that has never fetched it must fetch it once. `Package.resolved`
pins the revision; it does not carry the sources.

What is true, and what this leaf proves, is the clause those sentences exist for — **that
nothing in the build or the test run reaches the network**, Monaco included, which is the whole
reason the bundle is committed. The proof is a fresh clone tested under `sandbox-exec` with
`network*` denied, resolving `libghostty-spm` from SwiftPM's machine-level repository cache.

**For the architect at merge:** amend C7.2's G3 and Parent-Level Acceptance item 6 to say "with
no network beyond SwiftPM's resolution of the package's declared dependencies, which a warm
dependency cache serves". C7.1 and C7.3 inherit the same clause and the same bound.

## Parent revisions

Advisory overturns, for the architect to apply to the composite at merge. This leaf does not
edit the composite; three leaves share that file.

1. **S3 measures three load paths.** The composite's W4 delegated-unknown names the document and
   the workers. Add dynamic-import chunks, for the reason in Design §7, and name the degraded
   no-worker resort as a fourth so it cannot be reached silently.
2. **The Grounding Baseline's Monaco layout is stale for 0.56.0.** `esm/vs/…` specifiers,
   `editor.all.js` and the separate `basic-languages` and `language/*` contributions describe the
   layout `integrate-esm.md` documents, not the package that ships. Replace with this document's
   Grounding paragraph. The `integrate-esm.md` fact that still holds is the one W4 depends on:
   ESM integration requires `MonacoEnvironment.getWorker` or `getWorkerUrl`.
3. **The measured bundle size answers Question 1 of the human gate.** 13 MB, 107 files, 2.91 MB
   compressed, `ts.worker.js` 6.47 MB of it. The recommendation the gate was asked to rule on
   — commit it — holds on the measurement, and the named lever if a future bump breaks the
   budget is the TypeScript language service.

## Contracts

- **W4 Monaco bundle, bridge vocabulary and view.** Owned here. The canonical spelling of the
  message vocabulary is `Workbench/Sources/EditorCore`'s codec and the bootstrap it is tested
  against; C7.5 and C7.7 import it and never restate it.
- **W5 `LinkRouter` registration.** Owned here, and now delegated to by the app's
  `HostLinkRouter`. The specificity rule, the tie-break, the withdrawal and the fallback live in
  one place.
- **W1** manifest fence, respected: three edits inside `// MARK: - C7.2`, none outside it.
- **X7** consumed as C5 shipped it; no member of `PanelHostAPI` is added, removed or changed by
  this leaf, so `make check-x7` is unaffected.
- **X1, X11, §11, §6.3** as inherited.

## Delegated unknowns

**S3** as the composite states it, sharpened by Design §7. It runs as the first gate of the
Monaco half of this leaf and its artefact is that half's first deliverable, so a failure adopts
the next route in the list and re-runs, rather than discarding the work.

## Tech-debt entries

This leaf files under numbers **97–111**; a number outside that range belongs to another leaf.
Entries are written when the work that produces them lands, not reserved in advance.

## Decision Log

- Decision: `LinkRouting` depends on `PanelHostAPI`, and `LinkRouter` is built over X7's
  `LinkTarget` rather than a vocabulary of its own.
  Rationale: X7's 2026-09-06 amendment put the registry's vocabulary there; a second `LinkTarget`
  is the duplication the amendment exists to prevent. Rejected: re-declaring the vocabulary;
  a generic `LinkRouter<Tab>` whose parameter takes one argument for ever.
  Date/Author: 2026-09-07 / C7.2.

- Decision: `LinkRouter` is an actor, and the pop-out hook is a per-call `prepare` parameter
  rather than stored state.
  Rationale: the 2026-09-06 note kept `unregister(tab:)` `async` to leave this choice open, and
  serialised execution is what makes its ordering guarantee structural. A stored hook would make
  the actor hold the host that holds it. Rejected: a main-actor-isolated registry (simpler in
  three small ways and it forecloses the choice the note deliberately preserved); a stored
  `prepare` with a weak host capture.
  Date/Author: 2026-09-07 / C7.2.

- Decision: the bundle's entry point is `editor.main.js`, with code splitting on.
  Rationale: in 0.56.0 that entry is W4's contents exactly, so the tree-shaking follows from the
  entry rather than a curated import list that drifts at every bump; splitting keeps ~85 language
  grammars off the cold-load path. Rejected: reproducing `integrate-esm.md`'s per-contribution
  import list (it describes a layout the package no longer has); splitting off, which inlines the
  grammars into the document.
  Date/Author: 2026-09-07 / C7.2.

- Decision: the codec's discriminating test is a literal-JSON decode plus a bootstrap-source
  check, not the round trip alone.
  Rationale: the failure mode of a two-language wire format is disagreement between the two
  languages, and a Swift round trip cannot express it. §17.7 forbids a test that cannot fail.
  Rejected: the round trip alone; a JavaScript test runner in `swift test`, which would need a
  JS engine in the test target for one assertion.
  Date/Author: 2026-09-07 / C7.2.

- Decision: the bundle is committed on the measurement, without returning to the human gate.
  Rationale: the gate's instruction was to stop and report only above 20 MB; 13 MB was measured
  before the plan was written, so the condition for stopping is not met and the recommendation
  the gate already approved applies. Rejected: deferring the measurement to S3 and asking again
  (the measurement is ten minutes of work and the answer changes the plan).
  Date/Author: 2026-09-07 / C7.2.

## Surprises & Discoveries

- **Monaco 0.56.0 is not the package the composite's baseline describes.** The `exports` map
  rewrites every specifier, `editor.all.js` is gone, and `editor.main.js` now bundles the basic
  languages and the four web language features that used to be separate contributions. Following
  the baseline's spelling fails to resolve, which is how this was found rather than reasoned.
- **The bundle is 13 MB, and half of it is one file.** `ts.worker.js` is 6.47 MB minified. The
  10–20 MB the composite guessed was right, and the composition inside it — one worker dominating
  — is the part that matters, because it is the only lever if a future bump breaks the budget.
- **`HostLinkRouter`'s two fallback seams have never been exercised.** `openExternally` and
  `diagnostic` are settable for tests and no test sets them; the `.url` fallback and all five
  diagnostics are unproven in the app today. `LinkRoutingTests` covers them from this leaf on.
- **Code splitting turns the worker question into a chunk question too.** The composite's risk
  register names workers under a custom scheme; the same uncertainty applies to the dynamic
  imports that carry the language grammars, and nothing in the baseline had noticed.

## Outcomes & Retrospective

Pending — written when the leaf closes.

## Revision Notes

- 2026-09-07: v1, written at dispatch against the composite at `b775842`. Two `[parent-impact]`
  filings (W1's `LinkRouting` dependency row; the "no network" clause of G3 and Parent-Level
  Acceptance item 6) and three Parent revisions, none of them edited on the composite here.
- 2026-09-08: the gate's rulings, applied. Both `[parent-impact]` filings accepted by the
  architect: W1's row for `LinkRouting` gains `PanelHostAPI` at merge, and G3 with Parent-Level
  Acceptance item 6 are re-worded to the clause they exist for — nothing in the build or the test
  run reaches the network — with C7.1 and C7.3 inheriting the bound. The three Parent revisions
  are accepted. One ruling sharpens Design §7: **the degraded no-worker fourth resort is a stop,
  not a fallback.** If S3 lands there it is a Revision Note and a report to the architect, and the
  leaf does not promote past it on its own authority.
- 2026-09-08: **the bundle as built is 109 files and 13,082,833 bytes** (2.90 MB compressed),
  `ts.worker.js` 6.78 MB of it. `VERSION` deliberately carries no build date, so a rebuild of
  unchanged inputs is a no-op and `git status` stays clean; bun's content-hashed chunk names make
  the whole tree reproduce byte-for-byte. The `ts.worker.js` lever is recorded here so the next
  Monaco bump has the number in front of it: **dropping the TypeScript language service while
  keeping `json`, `css` and `html` removes about half the bundle**, and that is a W4 change rather
  than a local one.
- 2026-09-08: **the split build makes the workers thin shims, and that sharpens S3's third load
  path.** `editor.worker.js` is 156 bytes: with `--splitting` on, each worker entry `import`s
  shared chunks rather than carrying them. So chunks are fetched **from inside a worker context**,
  not only from the document, and the workers must be instantiated as module workers
  (`new Worker(url, {type: "module"})`).
  This matters most to the *second* route. A Blob-URL module worker has a `blob:` base URL, and a
  relative chunk specifier resolved against it does not point back at the scheme — so the
  composite's first fallback may fail for a reason that has nothing to do with the custom scheme
  and everything to do with code splitting. A lever exists and is S3's to use if it gets there:
  **build the workers in a second `bun build` invocation with splitting off**, which makes each
  worker a single self-contained script with no chunk fetches at all, at the cost of some
  duplication between the workers and the document. Recorded before S3 runs so the route is
  chosen on the measurement rather than improvised at the failure.
