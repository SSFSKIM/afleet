# C5: App shell, fleet browser, panel host and packaging (2026-09-06)

> **Parent:** `docs/doperpowers/specs/2026-09-03-afleet-workspace-design.md §17 C5`.
> **Parent-pin:** that path at commit `257822a` ("Spec: §17.9 C5 row unblocked by the C4
> merge"). **Level name:** child, wave 3 of the v1 roadmap. **Track:** controlled.
> **Branch:** `child/c5-app-shell`, worktree `../afleet-c5`; merges to `main` when G1
> through G4 pass. No gate of this child is blocked by another child: C1's fixtures, C2's
> wire layer, C3's index and C4's `LifecycleAPI`, `Fleet` and store are all on `main` at
> the pin.
>
> This document treats the parent's §17 C5 section and its inheritance as landed and
> records only the residue those sections leave to this child. It also reads the parent's
> Revision Notes of 2026-09-06 (the C3 and C4 reconciliations), the C3 child spec
> `2026-09-05-c3-fleetkit-timeline.md`, the C4 child spec
> `2026-09-05-c4-fleetkit-sessions-fleet.md`, the C7 composite spec
> `2026-09-05-c7-workbench-panels.md` and `docs/tech-debt-tracker.md` at the same commit.

## Purpose

At the pin, afleet is four Swift packages and a test suite. Nothing on this machine can be
launched and looked at. Everything the investigation established about the engine — that a
transcript can be read without owning the session, that a terminal's session can be watched
without being touched, that a fleet of sessions is a thing a person could browse — is true
only in tests.

C5 makes it a window. After C5 the user double-clicks an application, and every Claude Code
session on their machine is a row in a sidebar, grouped by project, marked with where it
lives: afleet's own, one running in their terminal, a background job, or a past transcript.
Clicking one shows what happened in it. A permission prompt waiting in any session appears
in one Activity list at the top of the sidebar and can be answered from there. A prompt that
arrives while the app is in the background raises a macOS notification carrying the engine's
own words. Cmd+K jumps to any session by name. Settings says which config home is in play,
which engine binary, which version, and what the environment resolved to — the four facts
that explain every surprise the app can produce. None of this requires the conversation
surface, which is C6's, so C5 ships the fleet browser and leaves a deliberately plain list
of item kinds where the conversation will go.

C5 also builds the two things the remaining children stand on. The first is the project
itself: an XcodeGen `project.yml`, an `Afleet` application target, a scheme, and a build
that runs from the command line, because until C5 there is no way to compile a line of UI.
The second is the **panel tab host** of contract X7 — the protocol by which C6 plugs in the
Thread and Agents tabs and C7 plugs in Files, Source Control, Terminal, Browser and GitHub.
Four of C7's seven leaves are blocked on it, and C6 is blocked on the whole shell. C5 is the
gate the rest of v1 comes through.

## Acceptance

The parent's four gates restated as behaviour a person or a test can observe, each with the
target that proves it. Three rules bind every one of them.

**Discriminating tests (parent §17.7).** A test written to prove a fix is demonstrated
failing against the pre-fix code before the fix is accepted. Where the failure cannot be
executed — a window that cannot open in a headless test runner — the substitute is stated as
such: a trace assertion on the model that would have driven it, plus a manual witness.
Reviewers ask what an assertion would have to *see* in order to fail.

**Nothing is written under a config home (X9).** Not by the app, not by a test. The default
suite builds its own scratch config home under `FileManager.default.temporaryDirectory`,
never the user's and never `/tmp/afleet-fixtures/config-home`. The one live leg (G1e) uses
`/tmp/afleet-fixtures/config-home` and runs a **recursive** before-and-after manifest of it —
relative path, size, mtime and inode for every file at any depth — diffed and then attributed
against a pinned allowlist of the paths a spawned `claude` legitimately writes. C4's G5 used a
top-level-name allowlist; that is not enough, because a defect that truncates `.claude.json`,
edits an existing transcript, or creates a file below an existing `projects/<slug>/` leaves the
top-level names unchanged. A filesystem diff also cannot say *who* wrote, since a legitimate
child is writing to the same tree, so the witness is paired with an app-side proof: every write
the app makes passes through one injectable file-operations seam, and a test asserts that seam
never receives a path under the config home while having received a non-zero number of writes
overall.

**Reports emit counts (parent §6.3, §11).** Every gate report, test failure message and
diagnostic in this child states counts, sizes, durations and identifiers — never a path, a
title, a session id or a slug from the author's home.

### G1 — it builds, and it lists the fleet (required)

**G1a — the build.** From a clean checkout of this branch:

```
xcodegen generate
xcodebuild -scheme afleet -configuration Debug build
```

prints `** BUILD SUCCEEDED **`. "Clean checkout" means a fresh `git clone` into a temporary
directory with no `DerivedData`, no `.build` and no generated `Afleet.xcodeproj`; the
generated project is not committed. This clause is also **every checkpoint's floor**: no
task in this child is complete while it fails.

**G1b — the suite.** `xcodebuild test -scheme afleet` runs the `AfleetTests` bundle *and*
the four packages' test targets in one invocation and reports zero failures. The scheme
names the package test targets explicitly (XcodeGen's `- package: <Package>/<TestTarget>`
form), so item 36's "`xcodebuild test -scheme afleet` passes" is one command rather than
five. `make test` is the same thing plus `make test-tools`.

**G1c — the listing, headless (default suite).** `SidebarModelTests` builds a scratch config
home holding a scripted set of transcripts across several project directories, with scripted
registry, roster and `agents --json` records, and drives `FleetBrowserModel` over a
`LifecycleAPI` double. Asserted: the set of listed channels equals the expected set;
`ListingPolicy`'s exclusions (a sidechain, a `continuedIn` transcript) are absent and each
absence names the rule that decided it; a teammate transcript is listed read-only; an
`entrypoint: sdk-cli` transcript — afleet's own, which the engine's own picker hides — is
listed; each row's origin glyph matches its `ChannelState.origin`; and the project sections
group by cwd with worktree sub-grouping. The set comparison is two-directional, so an extra
row fails as loudly as a missing one.

**G1d — first paint, opt-in measurement.** `LocalFleetPaintTests`, skipped unless
`AFLEET_LOCAL_INDEX=1`, reads the config home the environment resolves (read-only,
`O_NOFOLLOW`, no persisted snapshot) and measures the time from composition-root start to a
populated `FleetBrowserModel`, five runs, median under **five seconds**. It reports counts,
sizes and milliseconds only. Tracker entry 21 is the known hazard: the 365 ms index build C3
measured is warm steady state, and a genuinely cold page cache adds the read of some 380 MB
of heads and tails. The mitigation is in the design below (a persisted snapshot painted
first); this gate measures both the no-snapshot and the with-snapshot path and reports both
numbers.

**G1e — a foreign session, live, zero model turns.** `LiveForeignChannelTests`, skipped
unless `AFLEET_LIVE_CLI=1`, with `CLAUDE_CONFIG_DIR=/tmp/afleet-fixtures/config-home`: the
test starts an interactive `claude` as a child in a directory the scratch `.claude.json`
**already trusts**, read from that file the way C4's `LiveFleetTests.trustedDirectory(_:)` does
— entries under `/private/tmp/afleet-fixtures/` with `hasTrustDialogAccepted == true`, the
config home itself and anything beneath it excluded by code, the directory recreated if it is
gone and **trust never written**. A fresh temporary directory would be wrong: an interactive
session in a directory the home has never trusted sits on the workspace-trust dialog before it
writes any registry record, so the gate would fail or flake on the engine doing exactly what it
is designed to do — the same shape as the un-onboarded home sitting on the theme picker. The
test sends the child **no prompt**, and asserts that within five seconds the model lists that session with
origin `.foreignLive` and a presence read from its registry record; that afleet never
signals, stops or adopts it (a trace assertion that no termination path was entered, since
executing the break would kill the process the invariant protects); and that the scratch
config home's recursive manifest diff after the run contains **zero unattributed changes**:
every changed path matches the pinned allowlist of paths a spawned `claude` writes and is
attributed to the child the test started. The diff is not required to be empty — an unprompted
interactive session legitimately writes its registry record, and demanding emptiness would fail
the gate on the engine doing exactly what the scenario needs it to do. Emptiness is required
only of the app-write seam, which must receive no config-home path at all. The **manual witness
for item 14's UI half** is the same scenario driven from Terminal.app against the built app,
recorded as a count and an elapsed time.

### G2 — Activity, badges and notifications (required)

**G2a — Activity rows and answering (item 21).** `ActivityModelTests` replays `fake-claude`
against two scripted channels that each reach a `can_use_tool` decision, plus the
`rate-limited-turn` fixture and an `auth_status` frame. **The rate-limit row is driven by the
fixture's own `rate_limit_event`, never by a turn completing.** The installed 2.1.263 no
longer emits a per-turn event in the recorded scenarios (tracker entry 49, filed from the
2026-09-06 drift ritual), so a model that inferred a row from "a turn ended" would show rows
against a live engine that the fixtures never justified, and would show none where the engine
is quiet. The event is occasional; the row and the channel banner both live until the next
event replaces them, which is §7.6's rule. Asserted: Activity shows exactly
four rows with the expected `ActivityRow.Kind` values — two `.decision`, one
`.rateLimitInfo` and one `.authProblem` — because the parent's §7.6 lists auth state among
what Activity queries and a gate that counted three would let an auth failure stay invisible.
The `auth_status` frame carries an error, and a **second, healthy `auth_status` removes the
row**, which is the direction `ActivityQuery` already implements
(`FleetKit/Sources/FleetSessions/Activity/ActivityQuery.swift:123` appends and `:127` removes)
and which a one-directional test would not catch. **Answering the permission row from
Activity emits the control response** — the assertion is on the frame that left the host and
on the answering channel's `pendingDecisions` becoming empty, not on the row disappearing,
because a row can disappear for the wrong reason.

**G2b — badges (item 22).** A channel not in view with a pending decision carries a red
count badge and an unread dot; selecting the channel clears both, and the cursor survives a
model rebuild because it is persisted. Asserted over the model with a scripted clock.

**G2c — notifications (items 22 and 53).** The app posts through a `NotificationPosting`
seam. Asserted headlessly against a recording double: a pending decision in a channel not in
view posts one notification and the same decision in the selected channel posts none; the
`notification-hook` fixture's `hook_callback` for `afleet.notification` posts a notification
carrying the hook input's own `message` and `notification_type` **and** is answered with an
empty continue, so the engine is never left waiting. The **manual witness** is the built app
in the background raising a real macOS notification with the engine's text.

### G3 — the screens (required)

**G3a — the upgrade screen (item 33).** With the Developer binary override pointed at a
`fake-claude` that reports `2.1.200`, the app shows the upgrade screen naming the installed
version and the required baseline, and **opens no channel**: the assertion is that the route
is `.upgrade` *and* that the `Fleet` was never constructed and `start()` never called, which
is the clause that would actually catch a regression. Manual witness: the screen in the
built app.

**G3b — Settings (item 48).** With `CLAUDE_CONFIG_DIR` pointing at a scratch home holding
one session, Settings shows that resolved root and its source (`environment`), the sessions
listed come from it and none from `~/.claude`, and the readouts named in §6.5 are present:
protocol baseline, installed version, unknown-frame count since install, last census result.
It also shows the count of symlinked project directories the index skipped
(`TimelineNotice.indexBuilt`'s `symlinkedProjectsSkipped`), which is the number tracker entry
17 created so the app could say so rather than leave the difference invisible.

**G3c — the setup screen.** With no `claude` on the resolved PATH and no override, the app
shows the setup screen naming what is missing and offering *Check again*, and constructs no
`Fleet`. Same assertion shape as G3a.

### G4 — the panel host (required)

**G4a — the protocol and the order.** `PanelHostTests` registers stub tabs for several
`PanelTabID` cases in scrambled registration order and asserts the host presents them in the
canonical order (Thread, Agents, Files, Source Control, Terminal, Browser, GitHub); that
Cmd+1 through Cmd+7 select the Nth *registered and available* tab in that order; that a
second registration of one id is refused; and that a tab reporting itself unavailable for a
channel is absent for that channel and present for another.

**G4b — the channel context.** A stub tab reads every member X7 promises and asserts each:
the session id and cwd match the channel, the environment is the one ClaudeWire resolved,
the store handle writes and reads back under the `workbench` namespace **and cannot reach
another namespace**, a `WorkspaceLink` opened through the routing capability reaches the
registered target with the right `LinkDestination`, and the recent-URL feed returns the URLs
`ChannelTimeline.recentURLs(limit:)` returns for the same channel — asserted against a
timeline built from a fixture that contains URLs, so the feed is compared to a non-empty
expected list rather than to two empty ones.

**G4c — pop-out keeps its channel.** A placeholder tab pops out; the popped-out instance's
context resolves to the same channel; switching the main window to a different channel
leaves the popped-out window on its original one. Asserted over the pop-out registry and
context resolution headlessly; the window actually appearing is the manual witness.

**G4d — the pane seam (W8).** A `PaneRequest` handed to the host is delivered to the
registered pane runner **byte-for-byte unchanged, `id` included**, and the runner's
`PaneExit` reaches `LifecycleAPI.paneExited` with that same `id`. Asserted with a recording
lifecycle double; the discriminating clause is the `id`, because two requests with identical
other fields are two requests and C4 discards an exit whose id it is not waiting on.

## Live-cost statement

**C5's budget is zero model turns and zero dollars.**

- G2, G3 and the placeholder timeline are driven entirely by `Fixtures/` and
  `Tools/fake-claude`, which spend nothing.
- G1c and G1d read only. G1d reads the author's real config home, which is permitted —
  X9 forbids *writing* under a config home, not reading one — and is gated behind
  `AFLEET_LOCAL_INDEX=1` exactly as C3's G2 was, never runs in the default suite, and emits
  counts, sizes and timings only.
- G1e starts one real `claude` under the scratch config home
  `/tmp/afleet-fixtures/config-home` and **never prompts it**. An unprompted interactive
  session writes its registry record and ends without a `result` frame, so there is no turn
  and no cost. `AFLEET_LIVE_CLI_TURNS` is not read anywhere in this child; if a scenario ever
  needs a model turn, that is a scope change and goes back to the architect first.
- The scratch home is logged in and onboarded today (verified at the pin: `.claude.json`
  reports `hasCompletedOnboarding: true` and five projects). It lives under `/tmp` and dies
  with every reboot; the recovery step is one interactive `CLAUDE_CONFIG_DIR=/tmp/afleet-fixtures/config-home claude`
  run by hand, because `claude auth login` stores credentials without setting
  `hasCompletedOnboarding` (C4's finding, 2.1.263 `cli.pretty.js:265739-265749`). C5 expects
  this and G1e skips with that named reason rather than failing when the home is gone.

## Grounding

Measured in this worktree at the pin.

| Fact | Value |
|---|---|
| macOS / Xcode / Swift | 26.5.2 (25F84) / 26.6 (17F113) / 6.3.3 |
| XcodeGen | 2.46.0, installed |
| Installed CLI / protocol baseline | 2.1.263 / `ProtocolBaseline.version` = 2.1.259 |
| Extracted bundles | `~/claude-code-bundle/`, newest 2.1.263 |
| Packages | `AfleetCore`, `ClaudeWire`, `FleetKit`, `Workbench`, all `swift-tools-version: 6.2`, `platforms: [.macOS(.v26)]`, language mode 6 |
| Fixtures | 20 under `Fixtures/` |
| App scaffolding | **none**: no `project.yml`, no `.xcodeproj`, no `Info.plist`, no entitlements, zero files importing SwiftUI or AppKit, zero `@MainActor` declarations in any package |
| `Workbench` build | `swift build --package-path Workbench` succeeds in ~18 s including libghostty-spm and its `MSDisplayLink` dependency |
| `PanelHostAPI` | a target owned by C5 inside its own MARK fence in `Workbench/Package.swift`; the source file is a two-line placeholder; no test target exists for it |
| Tracker high-water | 51 after merging `main` (49 and 50 from the 2026-09-06 drift ritual, 51 from the router corrective), so C5's entries begin at **52** under a "From C5" heading |

**What the app is composed from.** Every seam the composition root needs is already public:
`EnvironmentResolver.resolve(shell:timeout:)`, `ConfigHome.derive(from:)`,
`BinaryLocator.locate(in:override:)`, `VersionGate.check(binary:environment:)` and
`ProtocolBaseline` in ClaudeWire; `TranscriptIndex(configHome:storage:reader:concurrency:diagnostics:)`,
`IndexStorage`, `TranscriptWatcher`, `StreamIngestion`, `ChannelTimeline.recentURLs(limit:)`
and `RegistryMirror` in `FleetTimeline`; `Fleet(configHome:environment:binary:store:diagnosticsDirectory:…)`,
`LifecycleAPI`, `ListingPolicy`, `ActivityQuery`, `FileStateStore`, `StoreIndexStorage`,
`FleetKitKeys`, `PaneRequest`/`PaneExit` and `JobEntry` in `FleetSessions`.

**Nothing in the four packages is `@MainActor`, `@Observable` or an `ObservableObject`, and
none of them imports SwiftUI.** Every change feed is an `AsyncStream` off an actor:
`LifecycleAPI.updates` (`ChannelState`, coalesced per channel), `LifecycleAPI.events(of:)`
(`WireEvent`, a fresh unbounded fan-out per call, nil unless owned, finished on archive),
`StreamIngestion.effects`, `TranscriptWatcher.changes`. `TranscriptIndex` has **no** change
stream at all: it is pull-only (`snapshot`, `entry(_:)`) with mutators that return values
(`build()`, `update(changed:) -> IndexDelta`). Owning the loop from the watcher into the
index and out to the sidebar is C5's job, and so is every `@MainActor @Observable` view model
in the app.

## Design inheritance

Graded as parent §17.3 grades it. Nothing below is re-litigated here; a clause this child
finds wrong is filed as `[parent-impact]`, never overridden locally.

**Binding, inherited as landed.** §5's five packages and their dependency edges, with the
app depending on all four (X1); §11's owned-files table and §12 in full (X9); §7.8 and X6's
store API, its three namespaces and the never-write rule; §7.1 through §7.4 **as a consumer**
— C5 reads C4's `ChannelState` and never re-derives an origin, an ownership decision or a
lifecycle transition; §8.4's answer mapping for the one card kind C5 answers; §9.6's
`WorkspaceLink` (X2); X4's timeline model and its recent-URL query; X5 as amended on
2026-09-05 and 2026-09-06, including `busy(LifecycleOperation)` and
`notEligible(Blocker)`; X11's single environment capture handed down to Workbench; and the
W1 skeleton's rule that a leaf edits only its own marked region of `Workbench/Package.swift`.

**Owned by this child.** X7, the panel tab host. §17.4 names C5 its owner and C7's cut
already fixed its location (the `PanelHostAPI` target) and added the recent-URL feed. The
concrete protocol is designed below and is filed as an amendment to X7 on the parent at
merge, not as a `[parent-impact]`.

**Advisory** — the parent's best thinking on child-local means, overturnable with evidence
and a dated Revision Note on the parent: §8.1's exact three-region layout and its use of
`NSSplitView`; §8.2's grouping thresholds, glyph choices and section order; §8.7's shortcut
table; §6.5's screen wording; and §11's prose about how the project is generated, short of
the owned-files table.

## Design

### 1. Packaging: `project.yml`, the app target and the scheme

`project.yml` at the repository root, generated but never committed
(`Afleet.xcodeproj/` is added to `.gitignore`, which already ignores `.build/`, `.swiftpm/`,
`DerivedData/` and `*.app`).

- **Project** `afleet`, deployment target macOS 26.0, Swift language mode 6 with complete
  strict concurrency on every target, matching the four packages.
- **Packages** `AfleetCore`, `ClaudeWire`, `FleetKit`, `Workbench` by relative path.
- **Target `Afleet`** (`com.apple.product-type.application`), sources under `App/`,
  dependencies on all four package products. It imports `Workbench`, whose umbrella
  `@_exported import`s `PanelHostAPI`, so the host protocol needs no product of its own.
- **Target `AfleetTests`** (unit-test bundle, `TEST_HOST` `Afleet`), sources under
  `AppTests/`.
- **Scheme `afleet`** — the name is the parent's, in G1 and in item 36, and is set as the
  scheme key rather than derived from the target name. Its build action builds `Afleet`; its
  test action lists `AfleetTests` and every package test target by
  `- package: <Package>/<TestTarget>`.
- **Signing**: ad-hoc (`CODE_SIGN_IDENTITY: "-"`, manual style, hardened runtime off, no
  entitlements file, so the app is not sandboxed and can read the user's config home and
  spawn processes under the resolved environment).
- **`Info.plist`** written by XcodeGen from `properties`: bundle identifier
  `com.afleet.app`, display name `afleet`, `LSMinimumSystemVersion 26.0`, no custom icon in
  C5.
- **`Makefile`** gains `generate`, `build`, `test` and `check-imports`. `test` is
  `xcodebuild test -scheme afleet` plus `make test-tools`; the per-package `swift test`
  invocations every earlier child used keep working unchanged.

**Contract X1's check.** The three existing packages each carry an `ImportGraphTests` that
greps their own sources against an allowed set with a floor assertion ("the grep found no
Swift files at all" and "the grep did not find the imports every file has"), so a silently
empty grep cannot pass. C5 adds two of the same shape: one over
`Workbench/Sources/PanelHostAPI` (allowed: Foundation, SwiftUI, AfleetCore, FleetKit and its
sub-modules; **ClaudeWire forbidden**, every panel target forbidden) and one over `App/`
(all four packages and system frameworks allowed). Both run inside `xcodebuild test -scheme
afleet`, which is X1's "CI check in C5 rejects a violating import" — this project runs no CI
service, so the merge gate's own command is the check.

### 2. The composition root

One `@MainActor` `AppModel` owns a state machine over four routes: `.launching`, `.setup`,
`.upgrade`, `.workspace`. Launch, entirely off the main actor except where noted:

1. `EnvironmentResolver().resolve(shell:)` through the login shell (§6.9, X11). Captured
   **once** per launch and handed to everything below, including every panel.
2. `ConfigHome.derive(from:)` — `CLAUDE_CONFIG_DIR` when set, else `<HOME>/.claude`.
3. **The write-root overlap check.** afleet has exactly two write roots,
   `~/Library/Application Support/afleet` for the store and `~/Library/Logs/afleet` for
   diagnostics. `CLAUDE_CONFIG_DIR` is an arbitrary path, so a home of `~/Library/Logs`, or any
   ancestor of either root, would put afleet's own files inside a config home and break X9
   before a child runs. `FileStateStore`'s `configHomes:` guard covers the store only; `Fleet`
   builds `FileDiagnostics` and `FileFleetDiagnostics` itself from the directory it is handed,
   eagerly and unguarded. So the check sits here, ahead of both: canonicalise all three paths,
   and if either write root is the config home or lies beneath it, route to the setup screen
   naming the collision and construct no store, no diagnostics and no `Fleet`.
4. `try FileStateStore(baseDirectory: <~/Library/Application Support/afleet>, configHomes: [configHome.root])`.
   The initialiser is throwing and its `configHomes` argument **is** the X9 guard: it
   canonicalises both paths and throws `StoreError.insideConfigHome` when the store would sit
   under a config home. Passing an empty array to make it compile would delete that
   protection, so the resolved config home is always passed. A throw routes to the setup
   screen naming the store error; a namespace whose document reports `SchemaStatus.newer`
   becomes a banner rather than a crash.
5. `BinaryLocator.locate(in:override:)`, the override read from the `afleet` namespace's
   Developer settings. `nil` → `.setup`, and no `Fleet` is constructed.
6. `VersionGate().check(binary:environment:)`. `.tooOld` → `.upgrade` naming both versions;
   `.unparseable` → `.setup` naming the probe's output; `.accepted` → continue.
7. `TranscriptIndex(configHome:storage: StoreIndexStorage(store:))`. `loadPersisted()`
   first — the sidebar paints from the previous launch's snapshot immediately, marked as
   restoring — then `build()` **in a detached task**, because C3 measured that an index build
   awaited from a main-actor-bound caller runs at about a third of the machine's width.
   `IndexSnapshot` replaces the painted rows when it lands. **The list may be stale; the
   glyphs may not.** A restored row's origin is not drawn from the snapshot: origins come only
   from `ChannelState`, which C4's holder observer seeds at supervisor build and refreshes on
   its five-second poll, so no origin outlives one poll. A restored row with no `ChannelState`
   yet renders without an origin glyph rather than with a remembered one. This is what closes
   tracker entry 21: the entry warns that the measured index cost is warm steady state, and
   the answer is that first paint no longer waits on the index at all.
8. `Fleet(configHome:environment:binary:store:diagnosticsDirectory: ~/Library/Logs/afleet)`
   and `start()`.
9. Registration, through the `WorkspaceCoordinating` seam (§3 below). The composition root
   drives it for the persisted snapshot, for the completed fresh build, and for every
   `IndexDelta` the watcher produces, **before** those rows are exposed as live — a channel
   `Fleet` has never been told about has no supervisor, and `events(of:)` returns nil for a key
   it does not know. A cold launch with no persisted snapshot must not finish with zero
   registered supervisors, which is the failure a warm-path-only wiring would hide.
10. `TranscriptWatcher(configHome:).start()`, its `changes` pumped into
   `index.update(changed:)` — whose `IndexDelta` patches the sidebar — and into
   `StreamIngestion.fileChanged(_:)` for the open channel. A Developer toggle leaves the
   watcher stopped, which is item 56's switch.
11. `LifecycleAPI.updates` consumed into the sidebar model for the life of the app.

**Diagnostics.** Parent §11 wants one metadata log under `~/Library/Logs/afleet/`. C2's
`DiagnosticEvent` and FleetKit's `TimelineNotice`/`FleetDiagnosticEvent` are three closed
enums in three packages that cannot see each other, and C3 recorded explicitly that
composing them is C5's. The app installs `FileFleetDiagnostics` and `FileDiagnostics` on the
same directory (which `Fleet` already does for two of them) and adds its own sink for
`TimelineNotice`, so a session's wire, fleet and timeline notices read as one recording. The
`~/Library/Logs/afleet/capture/` tree stays C2's and stays behind the Developer toggle.

### 3. Registration, and the one open scale question

`Fleet.register(_ key:cwd:recent:)` tells the fleet a channel exists, spawns nothing, and
reads the recency later when `perform(.open)` runs. It needs a **real cwd** — C4 is explicit
that a key with no seed runs in the config home and every precondition then refuses it — so
the seed is `IndexEntry.cwd` (already the relocated cwd where one exists), and `recent` is
§8.2's thirty-day rule over `IndexEntry.mtime`. C4's `Fleet.build` seeds a new supervisor
with the observer's current holder snapshot precisely because C5 registers from C3's index
and a pre-existing holder is the ordinary case; that seeding is a detached task, so **the
app drives the sidebar from `updates` and never from a `state(of:)` read taken immediately
after registering.**

The design registers every listed entry. The unknown is cost: the author's config home holds
3,032 transcripts, and registration builds one `ChannelSupervisor` actor and one detached
seeding task each. **Spike S-C5-2** measures registration of the full local set against G1d's
five-second budget before Task 2 commits to it. Its named fallback, pre-authorised so the
spike's failure is not a re-cut: register eagerly only the channels a holder names plus those
recent by §8.2's rule, render the rest from `IndexEntry` alone with origin `.archived` —
which is exactly §7.1's definition of archived, "otherwise" — and register lazily on
selection or on a holder appearing. If the fallback is taken it needs a fleet-wide holder
feed that `LifecycleAPI` does not expose today, which becomes an ask to C4 rather than a
local workaround.

Tracker entry 37 (a fork whose resolved session id is already registered overwrites the
entry instead of refusing; "the resolution belongs with C5") is the collision this
registration model makes reachable. C5's part is to not create it: registration is keyed by
`ChannelKey` from the index's own session ids, which are unique by construction, and a fork
re-key that collides is C4's `Fleet.publish`. C5 adds the case to its own registration tests
and leaves the fix with C4 unless the spike shows the app can provoke it.

### 4. The sidebar (fleet browser)

`@MainActor @Observable final class FleetBrowserModel`, fed by `IndexSnapshot`,
`IndexDelta` and `LifecycleAPI.updates`, joined per session id.

- **Which transcripts become rows** is not C5's decision: `ListingPolicy.include(_:)` decides,
  and the model records the deciding rule's `name` so a row's absence is explainable in the UI
  rather than mysterious. The rule order is C4's and is not second-guessed — in particular
  `own-sdk-cli` fires before `sidechain`, so afleet's own sessions are listed even when
  flagged, which is deliberate.
- **A row's static half** (title, `titleSource`, preview, relative time, cwd, git branch,
  agent name) comes from `IndexEntry`. **Its live half** (origin glyph, presence text, unread
  dot, pending-decision count, running glyph, banner, system item) comes from `ChannelState`.
  A row with no `ChannelState` is archived.
- **Sections are projects**, keyed by cwd, with worktree sub-grouping when one repository root
  holds several. `<configHome>/.claude.json`'s `projects` map is read read-only for display
  order and pinning; C4's `TrustReader` already reads that file the same way.
- **Thirty-day default**, the rest under *All projects*; **Background** lists `jobs()` with
  *Adopt*, *Attach* and *Stop*; **Archived** at the bottom, dimmed. Grouping, pinning and
  collapse persist through `FleetKitKeys.grouping`'s `SidebarGrouping` and unread cursors
  through `FleetKitKeys.unreadCursors` — both already modelled by C4, and reused rather than
  duplicated in the `afleet` namespace.
- **Actions on a row** go through X5 and nothing else. Two error cases C4 flagged for this
  child are handled by name: `LifecycleError.busy(op)` leaves the channel alone and tells the
  user to retry — **never an automatic retry** — and `LifecycleError.notEligible(blocker)` on
  *Reap* names the blocker (a turn, a decision, queued input, a wedge, or a background task
  by id) rather than saying "not now".
- **Cmd+K** is a fuzzy switcher over projects, channels and jobs, searching the whole index
  regardless of the thirty-day filter.

### 5. Activity

`ActivityQuery.rows(states:mirrors:recent:)` is a pure function C4 owns; C5 supplies its
three inputs. `states` is `LifecycleAPI.states()`. The other two come from **one
`events(of:)` subscription per owned channel** — bounded by C4's cap of six live processes —
which the app pumps into a bounded recent-frame ring (for `recent`) and into a C3
`RegistryMirror` (for `mirrors`, since `RegistryEntry` conforms to `TaskMirrorReading`).
That is a separate subscription from the one `StreamIngestion.open` consumes, which C3's tap
contract requires.

**Answering from Activity.** `LifecycleAPI` has no `answer` method; the action is
`perform(.answer(RequestID, InboundAnswer), on: key)`. C5 answers exactly one card kind: a
`can_use_tool` permission ask without `requires_user_interaction`, offering *Allow once*
(`allow`, `user_temporary`) and *Deny* (`deny`, `user_reject`) per §8.4's binding answer
mapping. Every other decision kind — question, plan, elicitation, dialog, and any ask
carrying `requires_user_interaction` — gets a row that names the kind and a *Go to channel*
action, because those cards are C6's and half a card is a wrong affordance, not a partial
one. **Activity shows a row for every decision kind** — the widening is in what a row can
*answer*, not in what it lists. `PendingDecision` carries no payload, so the payload for the card comes from the
`.request` event on the same subscription.

### 6. Notifications and keyboard

`protocol NotificationPosting` in the app, production over `UNUserNotificationCenter`, a
recording double in tests. Three sources, each toggleable in Settings: a pending decision in
a channel not in view; a completed turn in a channel not in view; and every notification the
engine raises through the `Notification` hook. The hook route is the important one and is
already wired below C5 — `InitializeConfiguration.afleetDefaults` registers
`afleet.notification`, and `InboundPolicy` *surfaces* a `hook_callback` whose id is
registered rather than answering it by policy — so the app posts from the hook input alone
(§8.7: `message`, `notification_type`, `session_id`, `cwd`, `transcript_path` are
display-ready) and then answers `.hookContinue(.empty)`, because an unanswered surfaced
request leaves the engine waiting.

**Spike S-C5-1** verifies that `UNUserNotificationCenter` authorises and delivers for an
ad-hoc-signed, non-notarised application launched from `DerivedData` on macOS 26.5, before
G2c depends on it. Named fallback: an in-app Activity banner plus a Dock badge, adopted with
a Revision Note on the parent, keeping the gate's observable ("the user is told about a
decision in a channel they are not looking at") intact.

Shortcuts implemented in C5, from §8.7's advisory table: Cmd+K switcher, Cmd+Shift+A
Activity, Cmd+1…7 panel tabs, Cmd+, Settings. Esc interrupt, Cmd+Enter send, Shift+Tab mode
cycling and Cmd+Shift+Esc stop-everything belong to the composer and the conversation
surface and are C6's; Cmd+Shift+T is C7's.

### 7. The panel tab host — contract X7, owned

Declared in `Workbench/Sources/PanelHostAPI/`, which may import Foundation, SwiftUI,
AfleetCore and FleetKit and **must not import ClaudeWire** (X1's edge for Workbench).

That last constraint is load-bearing and shapes the protocol. `LifecycleAPI`'s own signature
names `WireEvent`, a ClaudeWire type, so a `ChannelContext` that carried `any LifecycleAPI`
would drag ClaudeWire into Workbench. **The context therefore carries capabilities, not the
lifecycle actor**: narrow, host-provided closures and small protocols for exactly what a
panel may do. This is also the least-authority reading of W8's rule that the Terminal panel
never spawns `claude` on its own initiative — it cannot, because it is never handed anything
that could.

```
PanelTabID          a closed enum: thread, agents, files, sourceControl, terminal, browser,
                    github, in that canonical order, with a default title and system image.
                    Cmd+1…7 select the Nth registered-and-available tab in this order.

ChannelContext      key, session, cwd, environment (X11's single capture), a ScopedStore
                    bound to the workbench namespace, a LinkRouterCapability, a
                    RecentURLFeed, and a `reportPaneExit` capability. A value; the host
                    rebuilds it when the channel changes.

ScopedStore         read/write/remove/keys over one namespace, so a panel cannot reach
                    fleetKit's or afleet's documents.

RecentURLFeed       `current(limit:)` plus an updates stream, backed by
                    ChannelTimeline.recentURLs(limit:) — X4's query, so the Browser never
                    parses timeline items (X1).

LinkRouterCapability
                    register a LinkTarget for a WorkspaceLink predicate; unregister(tab:) to
                    drop every target a tab registered; open(link, from: .currentPanel |
                    .newWindow). The target's handler receives **both the link and the
                    destination**, because C7's W5 is binding and says so.
                    Cmd-click is .newWindow. C5's HostLinkRouter is the single registry in
                    the running app; when C7.2's LinkRouting target lands its reusable
                    registry the host delegates to it, and a second registry never exists.

PanelTabSession     @MainActor, AnyObject: the per-channel runtime state a tab owns — a
                    terminal's PTY and panes, an editor's open buffers and cursors. The host
                    retains one per (tab, channel).

PanelTab            @MainActor: id (an instance property, so `any PanelTab` can read it),
                    title, systemImage, isAvailable(in:), makeSession(for:) called once per
                    channel, and makeView(session:context:).

PanelHost           @MainActor: register(_:), unregister(_:) — async, because it awaits the
                    link-target withdrawal — available(for:) — the
                    registered tabs a channel can show, in canonical order — selected,
                    select(_:), selectIndex(_:), popOut(_:channel:), session(for:context:),
                    view(for:context:), registerPaneRunner(_:for:), run(_ request:
                    PaneRequest). Link opening is **not** a member: it lives on
                    LinkRouterCapability, which the host implements and hands into the
                    context, so a tab holds one routing seam rather than two. The host is
                    still what routes — .newWindow has to pop a tab out before the target
                    can deliver, and a tab cannot pop itself out — but it does so through
                    that conformance. **The canonical spelling of every member is the
                    source**, `Workbench/Sources/PanelHostAPI` — that is what C6 and C7
                    import — and `make check-x7` fails if this document names a member the
                    source does not ship, or omits one it does.

PaneRunning         run(_ request: PaneRequest) async — the Terminal tab's side of W8.
```

**The pane seam.** X5's `openInTerminal(key)`, `attach(job)` and `logs(job)` do the ownership
work and return a `PaneRequest`. The host routes it to the registered pane runner, selecting
or creating the Terminal tab, **unchanged, `id` included**. The runner reports the exit
through the context's `reportPaneExit`, which the host forwards to
`LifecycleAPI.paneExited(_:)` verbatim; C4 accepts an exit only when its `request.id` is the
one it is waiting on, and a late exit from an older pane is discarded. C5 neither invents nor
edits a request.

**Pop-out.** A SwiftUI `WindowGroup` over a `Codable, Hashable` value of tab id plus channel
key. The popped-out scene resolves its `ChannelContext` from the host by key, so the window
keeps the channel it was popped from when the main window moves on. Per-channel panel state —
which tab is selected, and Workbench's own `workbench.panel.<configHomeHash>.<sessionId>`
document — is the host's to key and the panel's to shape.

**One timeline registry for the app.** The recent-URL feed reads the same per-channel
`ChannelTimelineModel` the channel column renders, resolved from a single app-scoped
`ChannelTimelineRegistry` held on `AppModel`. Two registries would each ingest independently and
the Browser would watch a timeline nothing updated, which is the failure the feed exists to
prevent.

**Sessions are retained per tab and per channel; views are not.** C7's acceptance requires
that "panes and open editors are per channel and survive switching away and back". Caching the
`AnyView` *value* would guarantee nothing: SwiftUI owns `@State`, `@StateObject` and
representable coordinators through the rendered hierarchy and tears them down when a subtree
unmounts, so a tab holding its terminal in `@State` loses it whatever the host caches. The
contract is therefore a reference type — the host creates one `PanelTabSession` per
(tab, channel) on first render, retains it, and hands it back to `makeView` every time; a tab
puts its runtime state there. Each (tab, channel) also gets a stable SwiftUI identity so an
unrelated re-render does not discard the subtree.

**The session cache is bounded.** A host that retained a session for every channel a user
browsed would accumulate them across three thousand channels, and one that evicted on the
`.archived` origin would evict almost immediately, because `.archived` is the ordinary origin
of a registered channel with no live process — not a signal that the user is done with it. The
rule is therefore neither: sessions are held in an LRU of **sixteen** channels, with the
currently selected channel and every popped-out channel exempt from eviction, and a channel
removed from the index (an `IndexDelta.removed`) evicted at once. Sixteen is C4's live-process
cap of six plus room for the pop-outs and the recently visited; it is advisory and a measured
reason to change it is a Revision Note.

**Link routing is the host's, not the tab's.** `LinkRouterCapability.open(link, from:)` finds the most
specific registered target, and when the destination is `.newWindow` — which is what Cmd-click
maps to — pops that target's tab out for the current channel *first*, then delivers. A tab
cannot pop itself out, so the routing has to sit where pop-out lives. With no target
registered the fallback is `NSWorkspace.shared.open` for a `.url` and a diagnostic for
everything else, which is C7's W5 rule.

**`makeView` returns `AnyView`** because the host stores a heterogeneous list of tabs and an
`associatedtype Body: View` would make `PanelTab` non-existential. The cost is one type
erasure per tab per channel switch, which is not a hot path.

**Manifest.** C5 adds a `PanelHostAPITests` test target inside its own MARK fence in
`Workbench/Package.swift`, which W1 did not reserve. The fence is respected; the addition is
recorded as a Revision Note on the C7 spec so C7.1, which owns that file, knows the line
exists before its own branch opens.

### 8. The placeholder timeline

Deliberately plain, and replaced by C6. A header showing origin, presence, banner and system
item from `ChannelState`; a list over `ChannelTimeline.items` rendering each item's
`TimelineCategory`, timestamp and a one-line summary; no composer, no markdown, no cards.
An owned channel is driven by `StreamIngestion.open(file:events:)` over one `events(of:)`
subscription; an archived channel opens the same ingestion with an immediately-finished event
stream, so one code path serves both. The point of the view is to prove that a channel's
history reaches the screen from disk — item 1's UI half — not to render it well.

### 9. Settings

Environment (shell, capture mode, PATH entry count, captured-at); Engine (resolved binary,
installed version, protocol baseline, gate verdict, last census summary from
`FleetKitKeys.lastCensus`, unknown frame types seen since install); ConfigHome (root, source,
project count, transcript count, symlinked project directories skipped); Storage (schema
status per namespace, *Delete diagnostics*); Developer (binary path override, raw frame
capture, transcript watcher off, isolated settings for new channels, reveal the diagnostics
log). The Developer binary override is what item 33 points at a `fake-claude`.

## Contracts

- **X7, owned and specified above.** Filed as an amendment to the parent's X7 at merge, with
  the concrete protocol, the capability-rather-than-actor rule and its X1 rationale, the
  closed `PanelTabID`, and the pane seam's pass-through guarantee.
- **X1**, honoured by the app's dependency on all four packages and enforced by two new
  `ImportGraphTests`.
- **X9**, honoured: nothing this child ships or tests writes under a config home; G1e carries
  the before-and-after allowlist witness.
- **X11**, honoured: one `ResolvedEnvironment` per launch, carried into every
  `ChannelContext`.
- **X5, X6, X4** consumed as landed.

## Findings for the parent and the architect

Recorded now, none of them patched locally.

1. **The command-router strategies were unreachable through `LifecycleAPI` — closed on `main`
   before this child planned against the API.** C4's `StrategyExecutor.send` and
   `StrategyExecutor.run` took a `ChannelSupervisor`, and `Fleet`'s only accessor,
   `func channel(_ key: ChannelKey) -> ChannelSupervisor?`, is package-internal, so an app
   holding `any LifecycleAPI` could not execute `/login`, `/rewind`, `/permissions`, `/mcp` or
   `/memory` and had nothing to hand the `StrategyUI` it is meant to implement. C5 did not need
   it; C6 would have hit it on its first local command. The **C4 corrective** (`ace7a7b` code,
   `6e95fdd` docs) adds `route(_:on:)`, `send(_:on:)` and `run(_:arguments:on:ui:)` to both
   `Fleet` and `LifecycleAPI`, delegating to the router and `StrategyExecutor` with the
   supervisor's own handshake, `system/init` report and runtime state; FleetKit is at 417
   tests. `main` is merged into this branch and the plan is written against that API. No
   tracker entry: the gap is closed, not deferred.
2. **`SidebarGrouping` and `channels.unread` live in FleetKit's namespace** though X6 says
   FleetKit never models upper-layer state. They exist, they work, and C5 reuses them rather
   than duplicating the shapes in the `afleet` namespace. Recorded as an observation about
   X6's wording, not a request to move anything.
3. **`PanelHostAPI` has no test-target region in `Workbench/Package.swift`** (W1). C5 adds one
   inside its own fence; a Revision Note on the C7 spec tells C7.1.

## `[parent-impact]` — two writers cannot share the one diagnostics log §11 mandates

Filed for the architect, not worked around locally. It blocks no C5 gate and no remaining C5
task; it blocks the first consumer in C6 or C7 that emits a diagnostic.

**The binding clause.** §11's owned-files table — binding under §17.3 — gives afleet exactly one
`~/Library/Logs/afleet/diagnostics.log`. C3 assigned the composition to this child in as many
words: "the app composes both sinks into the one metadata log under `~/Library/Logs/afleet`,
which is C5's to wire."

**Why it cannot be wired.** Three closed diagnostic enums live in three packages that cannot see
each other, so there are necessarily three sinks. Two of them write that file:
`ClaudeWire`'s `FileDiagnostics` (`FileDiagnostics.swift:25-26`) and `FleetSessions`'s
`FileFleetDiagnostics` (`FleetDiagnostics.swift:142-143`). Both do the same thing — open with
`FileHandle(forWritingTo:)`, `seekToEnd()` **once**, then track their own offset. Two instances
on one path therefore overwrite each other rather than interleaving, and each rotation races the
other's offset. `Fleet` constructs its own sink internally from the `diagnosticsDirectory` it is
handed, so the app cannot substitute a shared writer without a signature change.

Nothing writes through the wire and fleet sinks today, so no line has been lost yet.

**Recommendation, and it is smaller than the obvious one.** Open the log with `O_APPEND` in both
sinks — `open(path, O_WRONLY | O_APPEND | O_CREAT, 0o600)` wrapped in
`FileHandle(fileDescriptor:)` — instead of seeking once and tracking an offset. Under `O_APPEND`
the kernel places every write at the current end atomically, so two writers interleave correctly
and neither needs to know the other exists. It is a few lines in each of C2 and C4, changes no
signature, breaks no child's API, and fixes the rotation race in the same stroke. The store next
door already works at this level (`DarwinStoreFileOperations`), so the idiom is not foreign to
the codebase.

Rejected: changing `Fleet.init` to take the two sinks rather than a directory, which C5's
executor proposed. It works, but it is a public signature change to a merged child's API that
C6 and C7 would inherit, to solve a problem that `O_APPEND` solves without one. Also rejected:
giving each sink its own file, which contradicts §11's table and would need a parent revision
rather than a corrective.

## Delegated unknowns

Parent §17.3 assigns C5 none of the numbered spikes. This child declares three of its own,
each with a named fallback so a failure is a Revision Note rather than a re-cut (§17.7).

| Spike | Question | Promote / discard criterion | Fallback |
|---|---|---|---|
| **S-C5-1** | Does `UNUserNotificationCenter` authorise and deliver for an ad-hoc-signed, non-notarised app launched from `DerivedData` on macOS 26.5? | A notification requested by the built app appears in Notification Centre | In-app Activity banner plus a Dock badge; Revision Note on §8.7 |
| **S-C5-2** | What does registering every listed channel cost at the local corpus's scale (3,032 transcripts, one supervisor and one detached seeding task each)? | First paint stays under G1d's five seconds, memory stays bounded | Eager registration only for holder-named and recent channels, lazy on selection; needs a fleet-wide holder feed from C4 |
| **S-C5-3** | Does `xcodebuild test -scheme afleet` run the four packages' test targets when the scheme lists them as `- package: <Package>/<TestTarget>`? | All five suites report in one invocation | `make test` runs `xcodebuild test` for the app plus `swift test` per package; item 36's single-command clause gets a Revision Note |

## Parent revisions

Filed for the architect to apply to the parent at merge. Nothing here is a local override.

### X7's amendment (this child owns X7)

> **X7 Panel tab host.** Amended 2026-09-06 from C5's build. The protocol lives in the
> `PanelHostAPI` target of the Workbench package and may import Foundation, SwiftUI,
> AfleetCore and FleetKit only. **The channel context a tab's view builder receives carries
> capabilities, never the lifecycle object**: the channel key, session id and cwd; the
> `ResolvedEnvironment` of X11; a store handle scoped to one namespace, so a panel cannot
> reach another package's document; a link-routing capability (`LinkRouterCapability`, named to avoid colliding with
> C7.2's module of the name `LinkRouting`, which a panel importing both would see, and whose
> target handler receives **both the link and the `LinkDestination`** as C7's binding W5
> requires; C5's `HostLinkRouter` is the app's single registry and C7.2's module is delegated
> to rather than duplicated); a
> recent-URL feed backed by X4's `ChannelTimeline.recentURLs(limit:)`; and a pane-exit reporter. `any LifecycleAPI` is
> deliberately absent because its signature names `WireEvent`, a ClaudeWire type, and X1
> forbids Workbench from importing ClaudeWire; the same absence is what makes W8's rule that
> a panel never spawns `claude` on its own initiative structural rather than a convention.
> The tab id set is **closed at the seven X7 already names** (Thread, Agents, Files, Source
> Control, Terminal, Browser, GitHub); the host orders by it and Cmd+1 through Cmd+7 select
> the Nth registered-and-available tab in that order; an eighth tab is a Revision Note.
> The host owns link routing itself: `open(link, from: .currentPanel | .newWindow)` finds the
> registered target, pops its tab out first when the destination is `.newWindow` (which is
> what Cmd-click maps to), and then delivers. The host **retains one `PanelTabSession` per
> (tab, channel)** — a reference type the tab defines and puts its runtime state in — and hands
> it back on every render, because caching a SwiftUI view *value* preserves nothing: SwiftUI
> owns `@State`, `@StateObject` and representable coordinators through the render tree and
> discards them when a subtree unmounts. That is how panes and open editors survive switching
> away and back, which is what C7's acceptance requires. The session cache is bounded by an LRU
> exempting the selected and every popped-out channel, so browsing three thousand channels does
> not accumulate three thousand sessions, and it is **not** keyed on the `.archived` origin,
> which is the ordinary origin of a registered channel with no live process. `PanelTab.id` is
> an instance property, and `PanelHost` carries `unregister(_:)`, which is **`async`**: it
> releases the tab's sessions and *awaits* `LinkRouterCapability.unregister(tab:)`, so a target
> never outlives the tab that registered it. The `async` is load-bearing rather than incidental
> — a synchronous member could only spawn the withdrawal and return, and on the handover path
> the method exists for, a withdrawal landing after the replacement registers removes the
> replacement's target, because withdrawal is keyed by a tab id both tabs share. So a later
> child can take an
> id C5's placeholder holds — without it `register`'s duplicate check would make the seven ids
> permanently first-come and C6 could never register Thread. A `PaneRequest` from X5 passes through the host to the registered pane
> runner unchanged, `id` included, and the runner's `PaneExit` returns to
> `LifecycleAPI.paneExited` unchanged, because C4 accepts an exit only when its `request.id`
> is the one it is waiting on.

Checked against every capability C7's composite already assumes of the host: W5's LinkRouter
registry with `.newWindow` on Cmd-click; W6's two store keys, one shared window-wide
(`workbench.browser`) and one per channel (`workbench.panel.<configHomeHash>.<sessionId>`),
both reachable through one namespace-scoped handle; W8's recent-URL feed and pane
request/exit; per-channel panes and editors surviving a channel switch; a pull request opening
in the Browser tab from the Source Control tab, which is cross-tab routing through the host;
and item 47's *Review trust in terminal*, which arrives as a pane request C6 emits. No
capability C7 assumes is missing from the shape above.

### Observations, for the architect to rule on at merge

- **`SidebarGrouping` and `channels.unread` live in FleetKit's `fleetKit` namespace** though
  X6 says FleetKit never models upper-layer state. C5 reuses them rather than duplicating the
  shapes in the `afleet` namespace. Whether X6's sentence moves or the code does is the
  architect's call at merge; C5 changes neither.
- **The setup screen is inheritance, not new design.** Its three states are §6.5's version
  gate (no engine, engine below the protocol baseline) plus the sign-in state C4 recorded
  (`claude auth login` stores credentials without setting `hasCompletedOnboarding`). Recorded
  here so §10's one-line "onboarding or upgrade screen" is not read as an unspecified screen.
- **The sidebar's thirty-day default is §7.4 and §8.2 applied at scale**, not a new policy:
  the parent already splits channels at thirty days, recent inside the project section and
  older under *Archived*. C4's `ListingPolicy` under X5 remains the ordering authority and is
  consumed, not re-implemented.

## Human-gate rulings, 2026-09-06

The eight questions of v1 were answered at the gate. Seven were settled by the parent and are
folded into the design above; one goes to the human without blocking this child.

| # | Question | Ruling |
|---|---|---|
| 1 | The onboarding screen | Accepted as one setup screen with three named states, each naming the terminal command that fixes it, no welcome tour. Recorded as §6.5 inheritance plus C4's sign-in finding. |
| 2 | The sidebar at three thousand sessions | Accepted, citing §7.4 and §8.2's existing thirty-day split rather than deciding afresh. `ListingPolicy` under X5 is the ordering authority. |
| 3 | A stale first paint | Accepted, with one added constraint: **the list may be stale but the glyphs must not be.** An origin never outlives one observer poll, because origins come from C4's holder observer, which seeds every registered channel at build. This closes tracker entry 21. |
| 4 | Activity answering | Accepted with a widening: **rows for every decision kind**, inline answer only for permission prompts (allow and deny) through `LifecycleAction.answer` under C4's request-id guards. Every other kind opens the channel, where C6's card lives. |
| 5 | The panel tab set closed at seven | Accepted; X7 names the seven and their shortcuts and exists so the late cuts inherit a fixed host. The capability-set resolution for the channel context is accepted and filed above as X7's amendment. |
| 6 | Bundle identifier and display name | **Open — the human's, asked and not blocking.** C5 proceeds with `com.afleet.app` and the display name `afleet`, defined in exactly one place, `project.yml`. It must be settled before the first build a real user runs, because notification permission follows the bundle identifier and changing it later resets that permission. |
| 7 | Reading the real config home | Confirmed: it is §1's premise, reading is permitted where writing never is, the default suite touches neither real home, and the one measurement that reads it stays opt-in and reports counts and timings only. |
| 8 | The zero-turn live test | Confirmed at zero model turns and zero dollars. Any spend comes back to the architect first. |

## Decision Log

- Decision: The panel `ChannelContext` carries narrow capabilities — a scoped store, a link
  router seam, a recent-URL feed, a pane-exit reporter — rather than `any LifecycleAPI`.
  Rationale: `LifecycleAPI`'s signature names `WireEvent`, a ClaudeWire type, and X1 forbids
  Workbench from importing ClaudeWire, so a context holding the lifecycle actor would break
  the package edge. It is also the least-authority reading of W8's rule that the Terminal
  panel never spawns `claude` on its own initiative: it cannot, because nothing it is handed
  could. Rejected: re-exporting ClaudeWire from FleetKit's umbrella, which would put a
  forbidden edge one import away from every panel.
  Date/Author: 2026-09-06, C5.
- Decision: this document's X7 section stays prose and a table; no fenced Swift block is added
  to make its effect claims machine-checkable, and `make check-x7` checks it for member
  *inventory* only — every shipped member mentioned, 24 of 24 — while the plan and the C7 note
  are checked declaration by declaration.
  Rationale: a signature lives in exactly one place that is executable, the source under
  `Workbench/Sources/PanelHostAPI`, which is what C6 and C7 actually import. A second Swift
  block here would be a third copy to drift, and this document's own purpose is to say *why*
  the shape is what it is, which no checker can verify anyway. What can go wrong in a prose
  document is that it describes a surface that no longer matches — a member gone, or one it
  never heard of — and inventory catches exactly that. The script says plainly that effects in
  a prose document are not machine-checked, rather than implying coverage it does not have,
  which is the failure the script itself was written after.
  Rejected: a fenced declaration block here, for the drift surface it would add; and dropping
  the document from the check entirely, which would let it describe a surface that no longer
  exists.
  Date/Author: 2026-09-06, C5, answering the question Task 2's executor escalated.
- Decision: `LinkRouterCapability` carries `unregister(tab:)`, and `PanelHost.unregister(_:)`
  calls it.
  Rationale: without it a `LinkTarget` outlives the tab that registered it, keeps winning the
  specificity contest, and delivers into a tab that is gone — the same asymmetry
  `PanelHost.unregister(_:)` was added to close, found by Task 2's review. Rejected: leaving it
  implicit in `PanelHost.unregister(_:)`'s contract with no protocol change, which works only
  while the app owns the registry; C7.2's `LinkRouting` module ships the real one and the host
  delegates to it, so the implicit version breaks exactly when C7.2 lands. The verb matches
  `PanelHost.unregister(_:)` deliberately: one word for one concept.
  Date/Author: 2026-09-06, C5, from Task 2's review.
- Decision: `PanelTabID` is a closed enum of seven cases and the host orders by it, rather
  than ordering by registration or by a string id.
  Rationale: X7 makes the host the owner of tab order and binds Cmd+1 through 7 to it; a
  closed set makes the ordering total and the shortcut mapping unambiguous, and it means C6
  and C7 inherit a fixed host rather than negotiating one, which is why X7 was named early.
  Rejected: open string ids with a declared sort key, which lets two leaves collide silently.
  Date/Author: 2026-09-06, C5.
- Decision: `PanelTab.makeView(session:context:)` returns `AnyView`, over a
  `PanelTabSession` the host created through `makeSession(for:)` and retains.
  Rationale: two decisions in one. `AnyView`, because the host stores a heterogeneous
  collection of tabs and an `associatedtype Body: View` would make the protocol
  non-existential and force a type-erasing wrapper anyway; one erasure per tab per channel
  switch is not a hot path. The **session**, because caching the view value would preserve
  nothing — SwiftUI owns `@State`, `@StateObject` and representable coordinators through the
  render tree and discards them when a subtree unmounts, so a tab holding its terminal in
  `@State` loses it whatever the host caches. Rejected: a generic host, which pushes the
  erasure to every call site instead of one; and caching `AnyView` per (tab, channel), which
  reads like state preservation and is not.
  Date/Author: 2026-09-06, C5, amended after the third adversarial pass.
- Decision: The app depends on the `Workbench` umbrella product rather than adding a
  `PanelHostAPI` library product to `Workbench/Package.swift`.
  Rationale: W1 designed the umbrella to `@_exported import PanelHostAPI`, so the app already
  gets the protocol; adding a product means editing the `products` array, which is outside
  C5's marked region in a file C7.1 owns. The cost is that the app's build links
  libghostty-spm from C5 onward, measured at ~18 s for a clean `Workbench` build, which is
  acceptable. Rejected for now: the extra product, held as the remedy if libghostty ever
  makes the shell build unworkable.
  Date/Author: 2026-09-06, C5.
- Decision: C5 reuses C4's `SidebarGrouping` and `channels.unread` in the `fleetKit`
  namespace instead of modelling sidebar state in the `afleet` namespace.
  Rationale: they exist, they are `Codable`, and duplicating the shapes would create two
  sources of truth for pinning and unread cursors. X6's sentence about FleetKit never
  modelling upper-layer state argues the other way; the observation is recorded rather than
  acted on, because moving a landed type is a change to C4 with no user-visible benefit.
  Date/Author: 2026-09-06, C5.
- Decision: The sidebar paints a persisted `IndexSnapshot` before the fresh build lands.
  Rationale: tracker entry 21 says the measured cold build is warm steady state, and G1's
  five-second claim is against a real first paint. Subject to Q3.
  Rejected: blocking the window on `build()`, which makes the worst case the only case.
  Date/Author: 2026-09-06, C5.
- Decision: C5's Activity answers permission asks only.
  Rationale: §8.4's cards are C6's, and an Activity row offering approximate affordances for
  a plan or an elicitation would let a user answer the wrong question. Subject to Q4.
  Date/Author: 2026-09-06, C5.
- Decision: Registration is eager over every listed channel, with lazy registration as a
  pre-authorised fallback behind spike S-C5-2.
  Rationale: C4 built `Fleet.register` for exactly this call and added its holder seeding
  because "C5 registers channels from C3's index"; the intended design is register-all, and
  the only question is cost, which is measurable rather than arguable.
  Date/Author: 2026-09-06, C5.

## Surprises & Discoveries

- Observation: S-C5-3 is answered and **promoted**. One `xcodebuild test -scheme afleet`
  invocation runs the app's own bundle and every package suite that has a test in it, with no
  second command and no `swift test`.
  Evidence: the run reported 11 test bundles and 676 tests, 16 of them skipped, 0 failures, in
  227 s: `AfleetTests` 2; `AfleetCoreTests` 6; `WireFramesTests` 61; `WireMCPTests` 13;
  `WireEnvironmentTests` 36; `WireDiagnosticsTests` 34; `WireTransportTests` 78;
  `ClaudeWireTests` 21 with 6 skipped; `FleetTimelineTests` 165 with 4 skipped;
  `FleetSessionsTests` 252 with 6 skipped; `PanelHostAPITests` 8. The 16 skips are the designed
  shape: the live-CLI tests skip without `AFLEET_LIVE_CLI=1` and FleetTimeline's four local-index
  tests skip without `AFLEET_LOCAL_INDEX=1`, and neither variable was set. FleetKit's 417 is the
  count the plan records, so nothing of FleetKit was lost in the crossing.
  The four remaining testables in the scheme — `TerminalCoreTests`, `EditorCoreTests`,
  `LinkRoutingTests` and `SourceControlCoreTests` — each ran and reported an unnamed `All tests`
  suite with `Executed 0 tests, with 0 failures`. Each of those four targets is a single source
  file containing the line `import XCTest` and no test case, so there was nothing to report.
  Two corrections to the first draft of this note, both from Task 1's review. It said they
  "printed no suite at all"; they did print, and that is **stronger** evidence than silence —
  a suite that runs and reports zero proves the scheme included it, where silence would have
  been consistent with exclusion. And the claim was checked twice rather than reasoned about:
  running `-only-testing:LinkRoutingTests` prints `Test Suite 'All tests' passed` with
  `Executed 0 tests`, and adding a temporary test case to `EditorCoreTests` made it report
  `Executed 1 test` through the scheme before being reverted. So the moment C7 puts a test in
  one, it reports with the rest.
  The gate the spike had to clear first: a `bundle.unit-test` target needs a plist to be code
  signed, and `xcodebuild test` refused the whole run — before executing anything — with "Cannot
  code sign because the target does not have an Info.plist file". `GENERATE_INFOPLIST_FILE: YES`
  on the test target settles it; the app's plist is a contract and is written out, a test
  bundle's is nobody's and is generated.

- Observation: `PanelHostAPI`, the target C5 owns, cannot name `any LifecycleAPI` without
  breaking X1.
  Evidence: `Workbench/Package.swift` gives `PanelHostAPI` dependencies `AfleetCore` and
  `FleetKit` only, with the file header stating "never `ClaudeWire`" (X1);
  `FleetKit/Sources/FleetKit/FleetKit.swift` re-exports `AfleetCore`, `FleetTimeline` and
  `FleetSessions` but **not** `ClaudeWire`; and `LifecycleAPI.events(of:)` returns
  `AsyncStream<WireEvent>?`, where `WireEvent` is `ClaudeWire/Sources/WireTransport/WireEvent.swift`.
  Nothing in Workbench references `LifecycleAPI` today, so the collision has never been
  compiled. It is the reason X7's context carries capabilities.
- Observation: `LifecycleAPI` has no `answer` method, despite the parent's 2026-09-06
  Revision Note reading "X5 gains `answer`".
  Evidence: `FleetKit/Sources/FleetSessions/Types/LifecycleAPI.swift` declares fifteen members
  and none is `answer`; the action is `LifecycleAction.answer(RequestID, InboundAnswer)`
  performed through `perform(_:on:)`. `ChannelSupervisor.answer(_:_:)` exists one level down
  and is not reachable from `Fleet`. The note and the code agree once read together; the
  spelling matters to anyone writing the Activity row.
- Observation: C4's command-router strategies cannot be executed through the public fleet API.
  Evidence: `StrategyExecutor.run(_:arguments:on:ui:)` takes a `ChannelSupervisor`; `Fleet`
  exposes no accessor for one. C5 does not need it; C6 will.
- Observation: the scratch config home survived and is usable at zero cost.
  Evidence: `/tmp/afleet-fixtures/config-home/.claude.json` reports
  `hasCompletedOnboarding: true` and five projects at the pin, so G1e needs no interactive
  re-login today.
- Observation: `TranscriptIndex` publishes no change stream, so the watcher-to-sidebar loop is
  the app's to own.
  Evidence: the actor exposes `snapshot`, `entry(_:)`, `build()`, `update(changed:)`,
  `loadPersisted()` and `persist()`, and nothing else; `TranscriptWatcher.changes` is a bare
  `AsyncStream<[URL]>`. C3 recorded the same division: "the watcher is a separate unit the app
  starts".
- Observation: two of this project's binding rules pull against each other on any test whose
  subject is a refusal, and the collision is not theoretical.
  Evidence: §17.7 requires a test be demonstrated failing against the pre-fix code; X9 forbids
  any write under a config home. For `TempTree`'s guard the pre-fix code *is* the guard
  removed, so the demonstration created a directory inside `/tmp/afleet-fixtures/config-home`
  (Task 1, twice, removed at once; no file inside created, modified or deleted, the thirteen
  top-level entries and everything under them intact, only the root's own mtime moved). The
  resolution is not to weaken either rule but to aim the demonstration at an invented path:
  the committed test now names a config home under the temporary directory, and the plan's
  Global Constraints carry the rule for Tasks 3, 8 and 9, which each hold a refusal test.
- Observation: the composition root of §2 compiles as specified under Swift 6 language mode,
  including the two clauses the final review corrected, so Task 3's central sequence is not
  resting on an untested reading of the API.
  Evidence: with `xcodebuild` down and no way to compile the app target, the eleven steps were
  written verbatim into a throwaway SwiftPM executable importing AfleetCore, ClaudeWire and
  FleetKit at `.swiftLanguageMode(.v6)`, and built clean in 13.4 s. Confirmed compilable:
  `try FileStateStore(baseDirectory:configHomes:)` with the config home passed as the X9 guard;
  the write-root overlap check ahead of it; `Task.detached { try? await index.build() }` issued
  from a `@MainActor` class, which is C3's requirement that the build not be awaited on the main
  actor; `TranscriptIndex(configHome:storage: StoreIndexStorage(store:))` and `loadPersisted()`;
  `Fleet.init(configHome:environment:binary:store:diagnosticsDirectory:)`, `start()` and
  `register(_:cwd:recent:)`; `TranscriptWatcher` behind `any TranscriptWatching` pumped into
  `update(changed:)`; `for await state: ChannelState in fleet.updates`;
  `ActivityQuery.rows(states:mirrors:recent:)`; pattern-matching `LifecycleError.busy` and
  `.notEligible`; and `ListingPolicy.include(ListingPolicy.IndexEntry(entry))` over a restored
  snapshot. The probe was deleted; it asserted nothing and ran nothing, and its only product is
  this note. What it does **not** cover is anything SwiftUI or app-target-shaped, which still
  waits on the Xcode floor.
- Observation: **a SwiftPM manifest does not enforce X1.** A target can import a module its
  manifest never declares, provided some other dependency pulled that module into the build, and
  it compiles clean. The grep test is not a backstop to the manifest; for this edge it is the
  only enforcement there is.
  Evidence: `Workbench/Package.swift` gives `PanelHostAPI` exactly two dependencies, AfleetCore
  and FleetKit, and the file's own header says "never `ClaudeWire`" (X1). Adding
  `import ClaudeWire` to a `PanelHostAPI` source nonetheless builds — SwiftPM resolves it
  transitively through FleetKit, whose package depends on it, and puts it on the import search
  path. Verified twice: once by Task 2's executor while demonstrating its import test failing,
  and once independently at the controller, `swift build --package-path Workbench` reporting
  `Build complete!` with the undeclared import in place, restored immediately after. The
  parent's X1 anticipated this — "a CI check in C5 rejects a violating import" — and this is why
  that sentence is load-bearing rather than belt-and-braces. It generalises past C5: every
  package edge in §5's table is enforced by its `ImportGraphTests` and by nothing else, so
  deleting one of those tests silently removes an architectural constraint rather than a test.
- Observation: a synchronous `unregister` spawning an asynchronous withdrawal fails
  **reproducibly under test and intermittently in production**, and the symptom is silence
  rather than misrouting.
  Evidence: the spawn-and-return shape was run five consecutive times and failed five times.
  The first draft of this note called that "deterministic, not a race"; the reviewer corrected
  it and the correction matters. The ordering is scheduler-dependent and stable only because
  the test process is otherwise idle, so a real app would see it intermittently — which makes
  the case for `async` stronger, not weaker, since an intermittent silent failure is the harder
  one to ever diagnose.
  The delivery log after a handover reads `["placeholder"]` — the second open reached *nobody*.
  Withdrawal is keyed by tab id and both the placeholder and its replacement carry `.thread`,
  so the late withdrawal removes the **replacement's** target and the panel is left with no
  handler and no error raised anywhere. Worse than the review predicted (delivery to the wrong
  tab), and the reason `PanelHost.unregister(_:)` is `async`: an `await` is a guarantee the
  compiler enforces, where "the host serialises its registry mutations" is prose that five
  implementers would each have to honour by reading.
- Observation: `URL.resolvingSymlinksInPath` normalises `/private/var` to `/var`, not the
  reverse, so the pair a macOS temporary directory sits on converges from either side and cannot
  be used to prove a path-comparison guard fails open.
  Evidence: measured — `/private/var/folders` and `/var/folders` both resolve to `/var/folders`,
  `/private/tmp` and `/tmp` both to `/tmp`, and `FileManager.default.temporaryDirectory` comes
  back unchanged. `TempTree`'s X9 guard did compare a resolved path against unresolved ones and
  did fail open, but the shape that triggers it is any *other* symlink — a user-made one, or a
  `CLAUDE_CONFIG_DIR` pointed through one — which Foundation resolves leaving no second
  spelling. Both the review that raised the finding and this controller repeated the inverted
  mechanism; the executor measured it. The test proposed on the wrong mechanism was built first
  and its own non-emptiness floor rejected it as non-discriminating before it could be committed
  as proof of nothing — the discriminating-test rule catching a controller's error, which is
  what it is for.

## Outcomes & Retrospective

Pending — written at finish.

## Revision Notes

- 2026-09-06, v1: first draft, written against the parent at `257822a` after reading §17.4's
  C5 section, §17.3, §5, §6.5, §7.1–§7.4, §7.6, §8.1–§8.4, §8.7, §10, §11, §12, §14's items
  14, 21, 22, 33, 35, 48 and 53, contracts X1–X11 including the 2026-09-06 Revision Note, the
  C3, C4 and C7 child specs, and `docs/tech-debt-tracker.md`; and after a read of the four
  packages' public API in this worktree. Records the residue only. Eight questions stand at
  the human gate; three spikes are declared with named fallbacks; three findings are filed for
  the parent and the architect.
- 2026-09-06, v2: the human gate's rulings folded in. Seven of the eight questions were
  settled by the parent and are now design rather than questions; the eighth, the bundle
  identifier, is recorded as open and non-blocking. Three clauses changed the design rather
  than confirming it: a restored row's **origin glyph** may never be stale even though its
  text may (ruling 3, which is what closes tracker 21); Activity lists a row for **every**
  decision kind and answers only permission prompts (ruling 4); and the panel host **caches a
  tab's view per channel** and **owns link routing itself**, both of which fell out of
  checking X7's shape against every capability C7's composite already assumes — a tab cannot
  pop itself out, and a terminal pane rebuilt on every channel switch would satisfy the
  protocol while failing C7's acceptance. New `## Parent revisions` section carries X7's
  amendment for the architect to apply at merge, plus the two observations and the
  inheritance notes the gate asked to be recorded. Finding 1 is rewritten as the brief for
  the C4 corrective, naming the two `StrategyExecutor` signatures, the package-internal
  `Fleet.channel(_:)` at `Fleet.swift:293`, and what a public member would have to be.
- 2026-09-06, v2.1: two corrections from the architect. C5's tech-debt entries begin at **51**,
  not 49, because entries 49 and 50 are landing on `main` from today's fixture drift ritual
  against the installed 2.1.263; the router-strategy finding is renumbered accordingly. And
  G2's rate-limit row is pinned to the fixture's own `rate_limit_event` rather than to a turn
  completing, because one of those two drifts is that the engine no longer emits a per-turn
  event in the recorded scenarios — the event is occasional, and a model that inferred a row
  from a finished turn would be right against neither the fixtures nor the engine.
- 2026-09-06, v2.2: X7's link-routing protocol is named `LinkRouterCapability`, not
  `LinkRouting`. C7.2 owns a Workbench *module* called `LinkRouting`, and a panel target that
  imports both it and `PanelHostAPI` would see a protocol and a module competing for one name.
  Found while fixing the interfaces for the plan.
- 2026-09-06, v2.3: `main` merged into the branch, bringing the C4 corrective that closes this
  child's first finding — `LifecycleAPI` now carries `route(_:on:)`, `send(_:on:)` and
  `run(_:arguments:on:ui:)`, so the command-router strategies are reachable from a surface
  holding the protocol and C6 will not hit that wall. The finding is rewritten as closed and
  cites the corrective instead of a tracker entry. C5's tech-debt entries now begin at **52**,
  since the corrective took 51. Also from the first adversarial pass: G2's Activity gate gains
  a fourth row, `.authProblem`, with the healthy-status recovery direction, because the parent
  lists auth state among what Activity queries and a three-row gate would let an auth failure
  stay invisible; and the composition root's store construction is corrected to
  `try FileStateStore(baseDirectory:configHomes:)`, whose `configHomes` argument **is** the X9
  guard that refuses a store beneath a config home — the previous single-argument spelling does
  not exist and an empty array would have deleted the protection.
- 2026-09-06, v2.4: the second and third adversarial passes' fixes folded into **this
  document**, which the plan defers to and which had been left carrying two superseded designs.
  X7's panel contract is rewritten around `PanelTabSession`: caching an `AnyView` value
  preserves nothing, because SwiftUI owns `@State`, `@StateObject` and representable
  coordinators through the render tree and discards them when a subtree unmounts, so the host
  retains a reference-type session per (tab, channel) instead and a tab keeps its runtime state
  there. The session cache is bounded by a sixteen-channel LRU exempting the selected and
  popped-out channels, because retaining one per browsed channel does not scale to three
  thousand and evicting on the `.archived` origin would evict almost everything — `.archived`
  is the ordinary origin of a registered channel with no live process, not a signal the user is
  finished with it. `PanelTab.id` is an instance property and `PanelHost` gains
  `unregister(_:)`. And X9's witness becomes a recursive, attributing manifest paired with an
  app-side write seam, because a top-level-name diff cannot see a truncated `.claude.json` or a
  write below an existing project directory, and no filesystem diff can distinguish afleet's
  write from the spawned child's.
- 2026-09-06, v2.7: G1e's live scenario runs its unprompted `claude` in a directory the scratch
  `.claude.json` already trusts, selected the way C4's `LiveFleetTests.trustedDirectory(_:)`
  selects one and never writing trust, rather than in a fresh temporary directory. Architect's
  correction at the plan read: an interactive session in an untrusted directory sits on the
  workspace-trust dialog before it writes any registry record, so the gate would have failed or
  flaked on the engine doing exactly what it is designed to do — the same failure shape as an
  un-onboarded home sitting on the theme picker, which C4 already paid for once.
- 2026-09-06, v2.8, **final**: the sixth and last adversarial pass returned three findings, all
  real, none debt and none dismissible, all folded. **X9 had a reachable hole**: the store's own
  `configHomes:` guard covers the store, but `Fleet` builds its two diagnostics sinks internally
  and eagerly from the directory it is handed, so a `CLAUDE_CONFIG_DIR` resolving to
  `~/Library/Logs` or any ancestor of a write root would have put afleet's files inside a config
  home before a child ran. The composition root now canonicalises both write roots against the
  resolved home first and refuses an overlap, constructing no store, no diagnostics and no
  `Fleet`. **The timeline registry had no named owner**, so two could exist and the Browser's
  feed could watch a model ingestion never touched; there is now exactly one, on `AppModel`.
  **X7 had diverged from C7's binding W5**: the link handler took only the link, dropping the
  `LinkDestination` that contract requires, and registry ownership was ambiguous between C5's
  host and C7.2's module. The handler now takes both, C5's `HostLinkRouter` is the app's single
  registry, and C7.2 is delegated to rather than duplicated. Review stops here by the
  architect's ruling: six passes, thirty-four findings, every one confirmed against the code and
  fixed. These last three are folded unreviewed, so Task 1's executor and reviewer should treat
  the write-root check in particular as new rather than settled.
- 2026-09-06, v2.9: X7 gains `LinkRouterCapability.unregister(tab:)`, called by
  `PanelHost.unregister(_:)`. Task 2's review found that a tab could be unregistered while the
  link targets it registered stayed in the registry, still winning on specificity and still
  delivering into a tab that no longer existed — the asymmetry `unregister` was added to close,
  reintroduced one protocol along. Folded into the X7 amendment for the architect to apply at
  merge, and into the C7 Revision Note, because C7.2's `LinkRouting` module implements the
  registry the host delegates to and inherits the method.
- 2026-09-06, v2.10: `PanelHost.unregister(_:)` is `async`. Task 2's re-review found that a
  synchronous host could only spawn the asynchronous withdrawal and return, so on the handover
  path the method exists for, a late withdrawal deletes the replacement's link target — measured
  as deterministic, five failures in five runs, with the link then reaching nobody at all. Also
  in this revision: the C7 Revision Note that v2.9 **claimed** to have filed is now actually
  written, covering both `unregister(tab:)` and this `async` change, because C7.2 ships the
  registry the host delegates to and would otherwise have inherited both without reading about
  either. v2.9's claim was false when written. Recording that here rather than quietly making it
  true, because the failure worth remembering is asserting a thing had been done instead of
  checking — the same shape as the three contract holes this task's review found, each a
  guarantee stated in prose and enforced by nothing.
- 2026-09-06, v2.11: `PanelHost.selectIndex(_:in:)` takes the channel it indexes over. Task 2's
  review found that a host holding its own notion of the current channel, beside the one the
  render path passes to `view(for:context:)`, has two sources of truth and no rule saying who
  sets the first — so Cmd+1 could resolve against a channel the user is not looking at. Passing
  the context makes the mismatch unrepresentable, the same resolution as `unregister`'s `async`.
  The stub that prompted it got simpler, losing its stored context.
  Also: `make check-x7` now guards this document by member inventory and the plan and the C7
  note declaration by declaration, and the canonical spelling is named as the source rather than
  the plan, because the plan is execution scaffolding and the source is what C6 and C7 import.

