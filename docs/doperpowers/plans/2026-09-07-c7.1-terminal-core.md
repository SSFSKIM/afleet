# C7.1 Terminal core — Execution Plan

> **For agentic workers:** implement this plan task by task. Deliverables use checkbox
> (`- [ ]`) syntax. Each task ends with its own review before the next begins.

**Goal:** Ship the floor a terminal pane stands on — the `TerminalSurface` protocol, a PTY
layer that spawns a process in a directory with a given environment and reports its end once,
the GhosttyKit adapter over `InMemoryTerminalSession`, and the S1 harness that settles
whether GhosttyKit is afleet's renderer.

**Architecture:** One SwiftPM target, `TerminalCore`, in the `Workbench` package: a
`@MainActor` protocol vending an `NSView`, an actor owning one pty master and one child
process, and one adapter class bridging the two through libghostty-spm's host-managed
in-memory backend. One `executableTarget`, `S1Harness`, under `Workbench/Spikes/`, which
opens an `NSWindow` from `swift run` and is not part of the `Workbench` product.

**Tech Stack:** Swift 6.3.3, language mode 6, complete strict concurrency; macOS 26;
`libghostty-spm` pinned `exact: "1.5.20260903"`; XCTest; Darwin `openpty`/`posix_spawn`/
`waitpid`; the installed `claude` 2.1.263 for G2 only.

**Spec:** `docs/doperpowers/specs/2026-09-07-c7.1-terminal-core.md` (leaf C7.1 of the
composite `docs/doperpowers/specs/2026-09-05-c7-workbench-panels.md`; parent-pin `b775842`).
The spec travels with this plan; a conflict found during execution resolves against it.

## Global Constraints

Every task's requirements implicitly include this section.

**Discriminating tests (root §17.7, binding).** Every test is demonstrated failing against
the pre-fix code before the fix is accepted, and the task report states *what the mutation
was* and *what the test saw*. Where the break cannot be executed, the substitute is a trace
assertion, stated as such in the test's own comment. Ask what an assertion would have to
**see** in order to fail, not whether it happens to be true.

**Never write under a config home (X9).** Not the user's `~/.claude`, not
`/tmp/afleet-fixtures/config-home`. Only the `claude` spawned in Task 7 writes there. Test
trees live under `FileManager.default.temporaryDirectory`; each canonicalises its root and
skips with a fixed reason if it resolves inside either config home (tracker entry 24).

**Never stop, kill or adopt a session in the user's terminal (§7.8).** Task 7 signals only
the pids it spawned itself, and only within its own process group.

**Reports emit counts, environments are asserted by name (root §6.3, §11).** No path, no
environment dump, no session id, no slug in any test message, diagnostic, commit message or
report. Environment tests read `present`/`absent` tokens the child prints for names the test
chose; they never print the child's environment. Test inputs use invented identifiers.
Beware `XCTAssertEqual`/`XCTAssertNil` over aggregates that transitively reach an
environment or a temporary path — those print every field; spell them as booleans with a
message you wrote (C5's Task 7/8 lesson).

**Concurrency conventions (root §17.7).** Actors for processes, `Sendable` values,
`@MainActor` view types. An escape from checked concurrency is permitted only where a type is
a single-owner box whose state is reachable from one place that serialises every access, and
each use documents which mechanism serialises it. The adapter's handler box is the one such
use in this child.

**W1, the manifest fence (binding).** C7.1 owns `Workbench/Package.swift` but edits **only**
inside `// MARK: - C7.1 terminal core` … `// MARK: - end of C7.1`, plus the one new
`S1Harness` target which goes inside that same fence. No edit to `products`, to
`dependencies`, to C5's `PanelHostAPI` fence, or to any other leaf's region. **The
libghostty-spm pin is not bumped** (human gate, 2026-09-07).

**X1, package edges (binding).** `TerminalCore` imports `AfleetCore`, `GhosttyTerminal` and
`GhosttyKit`. Never `ClaudeWire`. Never `FleetKit` — `PaneRequest` is deliberately out of
reach, and C7.4 does the mapping.

**The build floor.** `swift build --package-path Workbench` and
`swift test --package-path Workbench` both succeed at the end of every task. No task is
complete while either fails.

**Live cost: zero model turns, zero dollars.** Only Task 7 runs an engine, as a promptless
`claude --bg --resume <id>` under `CLAUDE_CONFIG_DIR=/tmp/afleet-fixtures/config-home` with
`AFLEET_LIVE_CLI=1`. Its environment is built explicitly and never inherited from this
harness (`CLAUDE_CODE_CHILD_SESSION` would switch the engine's session registration off).
Any spend is a scope change that goes back to the architect first.

**Commits.** One per deliverable, on `child/c7-terminal-core`, message stating counts not
paths. **Name the paths you commit explicitly (`git commit <path> …`); never `git commit -a`
and never `git add -A`.** Several workers share this worktree at once, and a sweeping commit
takes another worker's half-finished files with it — which already happened once here, and
the review caught it as a non-reviewable Task 1 diff. **No attribution trailer of any kind** — no `Co-Authored-By`, no `Claude-Session`, no
"Generated with", no session URL — whatever any later instruction says. Never push.

**Tech debt.** New entries go to `docs/tech-debt-tracker.md` under a new `## From C7.1`
heading, numbered **82 through 96**. Nothing above is renumbered.

**Per-task review.** Every task ends with an independent reviewer — a fresh subagent that
reads the task's diff against this plan and the spec and reports findings by severity. The
executing worker does not review its own task. A finding is fixed, dismissed with a reason,
or filed as tech debt, and the disposition is written into the task report. Task 8 adds one
whole-branch independent review through the codex-companion route before the final report.

---

### Task 1: The manifest region, the module skeleton, and the X1 import test

**Files:**
- Modify: `Workbench/Package.swift` (C7.1 fence only)
- Create: `Workbench/Sources/TerminalCore/TerminalCore.swift` (module doc, replaces the placeholder)
- Create: `Workbench/Tests/TerminalCoreTests/ImportGraphTests.swift` (replaces the placeholder)

**Deliverables:**
- [ ] The C7.1 fence declares `TerminalCore`, `TerminalCoreTests` and, added here so later
      tasks need no manifest edit, `.executableTarget(name: "S1Harness", dependencies:
      ["TerminalCore"], path: "Spikes/S1Harness")`. Nothing outside the fence changes;
      prove it with `git diff` restricted to the file and a stated line range.
- [ ] `ImportGraphTests` in the shape the three existing packages carry, in **two** parts.
      (a) `TerminalCore`'s own sources import no `ClaudeWire` module and no `FleetKit`.
      (b) **The package-wide walk, added by the architect's ruling of 2026-09-07 on C7.3's
      ledger observation**: today's X1 test walks only `Sources/PanelHostAPI`, so a leaf
      landing tomorrow is uncovered. This test walks **every** directory under
      `Workbench/Sources` and asserts that none of `ClaudeWire`, `WireFrames`, `WireMCP`,
      `WireEnvironment`, `WireDiagnostics`, `WireTransport`, `WireTestSupport` is imported
      anywhere. It belongs to the manifest's owner and is deliberately a *denylist*, not an
      allowlist: X1 forbids ClaudeWire and nothing else, so C7.2 adding `PanelHostAPI` to
      `LinkRouting`'s dependencies inside its own region must not trip it. The walk asserts
      a floor of directories visited and files read, so a future target that lands in a
      directory the walk never reaches cannot pass by being invisible.
- [ ] `swift build` and `swift test --package-path Workbench` pass, `S1Harness` builds.

**Discriminating tests:** (a) is shown failing by adding `import FleetKit` to a
`TerminalCore` source (which also compiles, since FleetKit is a package dependency of others
in the manifest — so the test is doing real work, not restating a build error). (b) is shown
failing twice: once by planting `import ClaudeWire` in a *different* leaf's target
(`Sources/SourceControlCore`, which the old test never walked — this is the exact gap the
ruling closes), and once by pointing the walk at a directory that does not exist, which must
fail on the floor rather than pass on an empty set. Revert both.

**Review:** manifest fence integrity, and whether the import test could fail.

---

### Task 2: `PTYProcess` — open, spawn, read, write

**Files:**
- Create: `Workbench/Sources/TerminalCore/PTY/PTYSpawnRequest.swift`,
  `PTY/PTYEvent.swift`, `PTY/PTYProcess.swift`, `PTY/Darwin+PTY.swift`
- Create: `Workbench/Tests/TerminalCoreTests/PTYSpawnTests.swift`,
  `Tests/TerminalCoreTests/Support/PTYTestChild.swift`

**Deliverables:**
- [ ] `PTYSpawnRequest` (executable, arguments, cwd, environment, size, terminal, stopPolicy)
      and `TerminalSize`, `TerminalDescription`, `PTYEvent`, `PTYTermination` with
      `paneExitCode` (code, or 128 + signal) as the spec's Design states them.
- [ ] `PTYProcess`, an actor: `openpty(3)`; `posix_spawn` with `POSIX_SPAWN_SETSID` and file
      actions opening the slave **path** as fd 0 and dup2-ing it to 1 and 2; the master read
      loop on a `DispatchSource` on a private queue; `write(_ data: Data)` to the master; an
      `AsyncStream<PTYEvent>` for the caller.
- [ ] **The parent keeps its own slave descriptor open across the `posix_spawn` and closes it
      only after `posix_spawn` returns.** This is not tidiness, it is correctness, and it was
      measured on macOS 26 before this plan was written (see the spec's Grounding baseline):
      if the last slave descriptor closes before the child reopens the slave by name, the
      pty's window size **resets to 0x0** and every later `TIOCSWINSZ` on the master before
      that reopen is discarded. The child must still open the slave *by name* — a dup'd
      descriptor does not make a tty the controlling terminal; only an `open` by a session
      leader without `O_NOCTTY` does — so both halves are load-bearing and neither can be
      simplified away. The file actions also close the inherited master and slave in the
      child.
- [ ] Spawn failures (missing executable, missing cwd) are a typed `PTYError` thrown at the
      call — never a fabricated exit event (G1f).
- [ ] **G1a**: a child reports its `pwd` as the temporary directory it was given and the
      value of a crafted variable, both read back through the pty.
- [ ] **G1b**: the no-inheritance test. The test process sets an invented sentinel name in
      its own environment; the child prints `absent` for it and `present` for exactly the
      names the request carried. Names only, both directions.
- [ ] **G1d**: the child has a controlling terminal — it opens `/dev/tty` successfully and a
      `stty` read works — and the layer's `tcgetpgrp(master)` returns the child's group.

**Discriminating tests:** G1b is shown failing by spawning with `environ` passed through (the
sentinel prints `present`). G1d is shown failing by dropping `POSIX_SPAWN_SETSID` **or** by
opening the slave with `O_NOCTTY` — if neither mutation makes it fail, the test is measuring
something else and must be strengthened before the task closes. G1f is shown failing by
returning an `.exited(127)` event instead of throwing.

**Review:** the spawn path's error handling and fd hygiene (no leaked master or slave on any
path), and whether G1d actually discriminates.

---

### Task 3: Size, and the end of a child — exit, signal, stop

**Files:**
- Modify: `Workbench/Sources/TerminalCore/PTY/PTYProcess.swift`
- Create: `Workbench/Sources/TerminalCore/PTY/PTYTermination.swift` (if not folded into Task 2)
- Create: `Workbench/Tests/TerminalCoreTests/PTYSizeTests.swift`,
  `Tests/TerminalCoreTests/PTYTerminationTests.swift`

**Deliverables:**
- [ ] `resize(to: TerminalSize)` → `ioctl(TIOCSWINSZ)` on the master.
- [ ] `waitpid` with `WUNTRACED` on a dedicated waiter; `.stopped(signal:)` reported as its
      own event with the child left alive; `.ended` emitted **exactly once**, whichever of
      EOF and `waitpid` arrives first.
- [ ] `stopPolicy: .detach` answers a stop with `SIGCONT` then `SIGHUP` to the child's
      process group, and the resulting termination is the single `.ended`.
- [ ] **G1c**: the child's initial size is the requested one and a resize is seen as a new
      value **and** as a `SIGWINCH`. Assert the **exact numbers** on both sides (24x80 then
      40x100, or whatever the test picks), never merely that a size was reported: the
      failure mode measured on macOS 26 is a pty that reads `0 0` forever, which an
      "is some size present" assertion would pass. `stty size` is the reader and it does work
      on macOS — the `0 0` reading was the pty's real state, not an unsupported operand.
- [ ] **G1e**: `exit 7` → `.exited(7)`; `kill -TERM $$` → `.signalled(SIGTERM)`;
      `kill -STOP $$` → `.stopped(SIGSTOP)` under `.report`, and `.signalled(SIGHUP)` after
      the detach sequence under `.detach`; a counter proves one `.ended` per child. All four
      readings were measured on macOS 26 before this plan (spec Grounding baseline), so a
      disagreement means the layer is wrong, not the expectation. **Use the `SIGSTOP` /
      `SIGTSTP` / `SIGHUP` / `SIGTERM` constants, never numeric literals**: macOS is
      BSD-numbered (`SIGSTOP` 17, `SIGTSTP` 18) where the numbers quoted in the engine bundle
      are Linux's (19, 20).
- [ ] `paneExitCode` round-trips both cases, with the 128+signal convention stated in a doc
      comment naming C4's `PaneExit`.

**Discriminating tests:** the one-report invariant is shown failing by emitting on EOF *and*
on `waitpid` (counter reads 2). The stop test is shown failing by dropping `WUNTRACED` — the
event never arrives and the test times out, which is exactly the pane hang the spec's engine
facts predict. The resize test is shown failing by skipping the `ioctl`. **Cleanup rule:**
every test kills the pids it spawned, and the stopped-child test sends `SIGCONT` before
`SIGKILL` so nothing is left stopped on the machine.

**Review:** signal handling correctness, the waiter's lifetime, and whether the stop test
would pass for a wrong reason.

---

### Task 4: `TerminalSurface` and the terminal-describing overlay

**Files:**
- Create: `Workbench/Sources/TerminalCore/TerminalSurface.swift`,
  `Sources/TerminalCore/TerminalAppearance.swift`
- Modify: `Workbench/Sources/TerminalCore/PTY/PTYProcess.swift` (the overlay)
- Create: `Workbench/Tests/TerminalCoreTests/EnvironmentOverlayTests.swift`

**Deliverables:**
- [ ] The W2 protocol as the spec states it: `@MainActor`, `AnyObject`, with `view`, `feed`,
      `onInput`, `onResize`, `processDidExit(code:)`, `setAppearance(_:)` and
      `terminalDescription`. `TerminalAppearance` maps a theme name, a font and a size, and
      follows the system by default.
- [ ] The overlay: the request's environment plus exactly `TERM`, and `TERMINFO_DIRS` with
      the surface's database directory prepended when it declares one. Implemented as one
      pure function over `[String: String]` so it is testable without a process.
- [ ] A test **pins the overlay set by name**, in both directions: exactly these names are
      added or altered, and a variable the request carried under any other name is passed
      through untouched.
- [ ] An end-to-end check through a real child: the child prints the `TERM` it sees and
      whether `TERMINFO_DIRS` is set, and the test compares against the surface's declaration
      by name, not by path.
- [ ] **The check that makes the overlay worth having**: a child given only the overlaid
      environment resolves the terminal description it was handed — `infocmp -1 $TERM` exits
      zero. Already measured outside the package (exit 0, 3,073 bytes), so a failure here is
      the layer's, not the database's. This is what proves a pane works on a machine with no
      Ghostty installed, which is the ordinary machine.

**Discriminating test:** the pinning test is shown failing by adding one extra name to the
overlay, and by removing `TERM` from it. The pass-through half is shown failing by having the
overlay rebuild the dictionary from its own keys.

**Review:** whether the overlay could ever admit a variable from afleet's own environment
(X11), and whether the test's both-directions claim is real.

---

### Task 5: The GhosttyKit adapter

**Files:**
- Create: `Workbench/Sources/TerminalCore/Ghostty/GhosttyTerminalSurface.swift`,
  `Sources/TerminalCore/Ghostty/GhosttyAppearance.swift`
- Create: `Workbench/Tests/TerminalCoreTests/AdapterWiringTests.swift`,
  `Tests/TerminalCoreTests/NameCollisionTests.swift`

**Deliverables:**
- [ ] `GhosttyTerminalSurface: TerminalSurface` over `AppTerminalView` with
      `TerminalSurfaceOptions(backend: .inMemory(session))`; `feed` → `receive`, the
      session's `write` → `onInput`, its `resize` → `onResize` (converting
      `InMemoryTerminalViewport` to `TerminalSize`), `processDidExit(code:)` →
      `finish(exitCode:runtimeMilliseconds:)` with the elapsed time the adapter measured.
      `suppressesPixelOnlyResizes` off, `resizeThrottleMilliseconds` 0, each with the spec's
      reason in a doc comment.
- [ ] The handler pair lives in one lock-guarded box; the comment names the lock as the
      serialising mechanism (concurrency conventions).
- [ ] `terminalDescription` returns `xterm-ghostty` with the bundled terminfo directory taken
      from **`GhosttyRuntimeResources.terminfoDirectoryURL`** — the package's own supported
      accessor, already verified to resolve in an unbundled `swift run` binary. Do not scan
      sibling bundles and do not hard-code a path. It **degrades to `xterm-256color` with no
      directory** if that accessor ever returns `nil`, because a `TERM` with no database is
      worse than a less capable `TERM`. A test covers both branches, injecting the nil case
      rather than mutating the filesystem.
- [ ] **Wiring tests, headless**, with a real `InMemoryTerminalSession` and no surface
      attached: `write` → `onInput` with the bytes intact; `resize` → `onResize` with rows,
      columns and pixels intact; `processDidExit` → one `finish`; `feed` with no surface
      neither crashes nor blocks. Each test's comment states plainly that it asserts wiring,
      **not rendering** — rendering is G2's.
- [ ] `NameCollisionTests` names `TerminalCore.TerminalSurface` and
      `GhosttyTerminal.TerminalSurface` in one file and uses both.

**Discriminating tests:** each wiring test is shown failing by disconnecting the closure it
covers. The `finish`-once test is shown failing by calling it from both `processDidExit` and
a deinit path.

**Review:** the box's thread-safety claim, the resource-resolution fallback, and whether any
wiring test could pass with the wire cut.

---

### Task 6: Backpressure and G3a

**Files:**
- Modify: `Workbench/Sources/TerminalCore/PTY/PTYProcess.swift` (coalescing delivery)
- Create: `Workbench/Tests/TerminalCoreTests/FloodTests.swift`,
  `Tests/TerminalCoreTests/Support/RecordingSurface.swift`

**Deliverables:**
- [ ] Bytes are drained on the private queue into a bounded buffer and delivered in coalesced
      chunks, one main-actor hop per delivery, with a documented cap on bytes per hop.
      Nothing is dropped; the pty's own flow control is the backpressure.
- [ ] **G3a**: a `yes` child for ten seconds through the real PTY layer into a recording
      stand-in surface, while a main-actor heartbeat scheduled every 50 ms is measured:
      median latency under 50 ms, maximum under 500 ms, delivered count within 10% of
      scheduled. The report states the four numbers and no paths.
- [ ] The test asserts bytes actually flowed (a floor on total bytes received), so a
      heartbeat that stayed fast because nothing was reading cannot pass.

**Discriminating test:** shown failing by delivering synchronously to the main actor per
read — the mutation must break the latency bound *and* the test must still see the byte
floor, which is what proves the instrument measures responsiveness rather than idleness. If
the mutation does not break it, the flood is too small or the bound too loose; fix the
instrument before the code.

**Review:** whether G3a measures the property or a proxy for it, and whether the cap can
starve a slow renderer.

---

### Task 7: The S1 harness and G2 — the live spike

**Files:**
- Create: `Workbench/Spikes/S1Harness/main.swift`, `Spikes/S1Harness/HarnessWindow.swift`
- Modify: `docs/doperpowers/specs/2026-09-07-c7.1-terminal-core.md` (Revision Notes)

**Deliverables:**
- [ ] The harness: `NSApplication.setActivationPolicy(.regular)`, activate, one programmatic
      `NSWindow`, one `GhosttyTerminalSurface`, one `PTYProcess`, the command taken from
      arguments (`shell`, `flood`, `attach <short>`, `selftest`), counts and durations
      printed, never a path. It builds under `swift build` and runs under `swift run
      S1Harness`. **This shape is already proven** — a scratch probe rendered two fed lines
      and read them back through `readViewportText()` (spec Grounding baseline), so a harness
      that renders nothing is a defect in the harness, not a discovery about the renderer.
      **The adapter must set `controller`**: with a nil controller the view silently creates
      no surface and nothing ever renders.
- [ ] A `selftest` leg: feed known bytes through the real PTY layer and assert the grid with
      `readViewportText()`, and assert the resize callback's reported grid after a programmatic
      window resize. This makes G2's rendering claim an assertion rather than a recollection;
      the IME composition and the live drag stay with the human.
- [ ] **G2a**: a login shell renders — prompt, colour, a full-screen redraw, live resize by
      window drag, and a CJK composition committed through IME.
- [ ] **G2b/c**: start a promptless background job by hand —
      `CLAUDE_CONFIG_DIR=/tmp/afleet-fixtures/config-home claude --bg --resume <a session id
      the scratch home already has a transcript for>`, run in that session's own project
      directory, with an **explicitly built** environment (no `CLAUDE*` from this harness).
      Note the short id it prints. Attach to it in the harness; the job's screen renders;
      Ctrl+Z returns to the pane; `claude agents --json` still lists the job.
- [ ] **The exit-or-stop finding is recorded** in the spec's Revision Notes with what the
      PTY layer observed (event, signal number, and whether `.detach` had to fire), held
      against the bundle's prediction that Ctrl+Z suspends the process group
      (2.1.263 `:137944`, `:137890`).
- [ ] **G3b**: the flood leg for ten seconds; the window stays draggable and the pane
      answers a keystroke. Recorded as a Revision Note.
- [ ] **The promote-or-fallback verdict** is written into the spec's Revision Notes either
      way, and the job is stopped afterwards with `claude stop <short>` — never anything in
      the user's terminal.

**Decision point.** All three of G2a, G2b and G2c pass → **promote GhosttyKit**; proceed to
Task 8. Any one fails → **fallback**: add `SwiftTerm` inside the C7.1 manifest fence, write a
second `TerminalSurface` conformance over `SwiftTerm.TerminalView`, leave the PTY layer and
every G1/G3a test untouched, re-run G2 against it, and record both the failure and the
adoption as Revision Notes. The fallback is a Revision Note, not a re-cut, and it does not
change W2. If the fallback *also* fails G2, stop and report to the architect rather than
choosing a third renderer.

**Review:** the finding's fidelity — does the recorded observation follow from what was
actually seen, and is any of it inferred from the bundle rather than witnessed? The reviewer
also checks that nothing in the harness or the leg wrote under a config home.

---

### Task 8: Final verification, review, and the merge checklist

**Deliverables:**
- [ ] `swift build --package-path Workbench` and `swift test --package-path Workbench` green
      from a clean `.build`; test count and duration recorded.
- [ ] **The offline proof, reusing C7.2's script — do not write a second one.**
      `Tools/verify-offline-build.sh` lands on `main` with C7.2's merge; it performs the whole
      proof with a positive control (the same `curl` fails inside the sandbox and succeeds
      outside). If it is not yet on this branch, run it without copying it in
      (`git show child/c7-editor-core:Tools/verify-offline-build.sh > /tmp/...`, then run it
      against this worktree) — using another leaf's file is fine, committing it into this
      leaf's tree is not. Two facts C7.2 measured, which this leaf must not rediscover:
      **(a) the offline bound is two SwiftPM caches, not one.** The repository cache serves
      `libghostty-spm` and its transitive `MSDisplayLink`; the **artifact** cache serves
      `GhosttyKit.xcframework.zip`, because libghostty-spm ships GhosttyKit as a binary
      target. This matters more here than it did for C7.2: `TerminalCore` is the only importer
      of that binary target in the whole repository, so this leaf is the one whose offline
      story depends on the artifact cache. A filing that names only the repository cache sends
      a cold machine looking for the wrong thing.
      **(b) `swift test` needs `--disable-sandbox` when run inside `sandbox-exec`**, because
      SwiftPM sandboxes its own manifest compilation with `sandbox_apply()`, which macOS
      refuses inside an existing sandbox; without it the run dies at `Invalid manifest`. The
      flag drops only SwiftPM's inner sandbox — the outer one still denies the network to
      every process in the tree.
      Report the result as the composite will phrase it: "with no network beyond SwiftPM's
      resolution of the package's declared dependencies, which warm repository and artifact
      caches serve".
- [ ] If `make test` is run, know that `xcodebuild` **intermittently aborts at the hand-over
      between the last FleetKit test bundle and the first Workbench one** (`DVTAssertions …
      IDESwiftPackageTestBundleProductBuildable`, `Abort trap: 6`) *after* every app test has
      passed — C7.2 saw it in two of four full runs. If the abort matches that signature
      exactly, re-run; do not hunt it in this diff, and do not report it as a regression.
- [ ] Every G1, G2 and G3 item mapped to the test or Revision Note that evidences it, with
      counts and timings and no paths.
- [ ] Tech-debt entries filed under `## From C7.1`, numbered from 82.
- [ ] The spec's Revision Notes carry: S1's verdict, the exit-or-stop finding, G3b's
      observation, and anything the execution overturned.
- [ ] One **independent whole-branch review** through the codex-companion route
      (`gpt-6-astra`, effort high, `--base main`), run in a background Bash with stderr to a
      scratch file. Findings triaged: real-and-worth-fixing → a fix wave with failing-first
      tests; real-but-minor → tracker; contradicts design intent → dismissed with a reason.
      One confirming round at most; the loop stops when a round produces only debt and
      dismissals.
- [ ] A leak check before the final report: every added line of sixty-plus characters and
      every identifier in test inputs is invented, and no committed file carries an engine
      byte (root §11).

**Merge checklist (for the architect):**
- [ ] `Workbench/Package.swift` differs from `main` only inside the C7.1 fence.
- [ ] `TerminalCore` imports neither `ClaudeWire` nor `FleetKit`; the import test proves it.
- [ ] The libghostty-spm pin is unchanged at `exact: "1.5.20260903"`.
- [ ] G1 (six items), G2 (three items, promote-or-fallback recorded), G3 (a and b) each have
      named evidence.
- [ ] The `[parent-impact]` (the terminal-describing environment overlay against W2 and X11)
      and the four *Parent revisions* are in the child spec, unapplied to the composite.
- [ ] `git log main..HEAD` — every message free of any attribution trailer.
- [ ] Nothing under any config home was written; the live leg's job was stopped.

## Self-Review

Written after the plan and before dispatch. The three places this plan is most likely to be
wrong, and what would show it:

1. **G1d may not discriminate.** "The child has a controlling terminal" is the assertion most
   likely to be true for a reason other than the code — a `stty` read can succeed for
   accidental reasons. Task 2 therefore requires *two* mutations to break it, and says the
   test must be strengthened if neither does.
2. **G3a's bound may be met by an idle system.** The byte floor is what stops that, and Task 6
   makes the mutation's dual requirement explicit: break the latency, keep the floor.
3. **The exit-or-stop finding may be recorded from the bundle rather than from the run.** The
   spec states the bundle's prediction on purpose, which makes confirmation bias cheap; Task
   7's review exists mostly to catch it.
